import { AgentDescriptor } from '../types/index.js';

export const AGENT_REGISTRY: AgentDescriptor[] = [
  // Research & Intelligence (20)
  {
    id: 'res-01',
    name: 'Market Research Agent',
    category: 'RESEARCH_INTELLIGENCE',
    role: 'Analyzes macro market dynamics, market sizing, and industry growth rates in Indian metro & tier-2/3 hubs.',
    systemInstruction: `You are the ${'Market Research Agent'}, operating within an autonomous AI Marketing Organization. Your core role is: ${'Analyzes macro market dynamics, market sizing, and industry growth rates in Indian metro & tier-2/3 hubs.'}. Always deliver evidence-backed, actionable, and structured outputs tailored to the target market and business goals.`,
    capabilities: ['data_analysis', 'structured_synthesis', 'domain_reasoning'],
    allowedTools: ['database_read', 'database_write', 'evidence_retrieval', 'analytics_query'],
    inputSchema: { type: 'object', properties: { context: { type: 'string' }, goal: { type: 'object' } }, required: ['context'] },
    outputSchema: { type: 'object', properties: { findings: { type: 'array' }, recommendation: { type: 'string' }, confidence: { type: 'number' } }, required: ['recommendation', 'confidence'] },
    confidenceThreshold: 0.8,
    taskPermissions: ['READ_ONLY', 'PROPOSE_DECISION'],
    costBudget: 500,
    executionLimits: { maxRetries: 3, timeoutSeconds: 60, maxTokens: 4096 },
    performanceMetrics: {
      completionRate: 0.98,
      approvalRate: 0.95,
      avgLatencyMs: 1200,
      totalExecutions: 24,
      successfulExecutions: 23,
      failedExecutions: 1,
      costIncurredINR: 4.5
    },
    memoryScope: 'MARKET',
    evaluationCriteria: ['Factual accuracy', 'Strategic relevance', 'Actionability', 'Compliance safety'],
    failurePolicy: 'RETRY',
    retryPolicy: { maxAttempts: 3, backoffBaseMs: 1000 },
    status: 'IDLE',
    version: '1.0.0'
  },
  {
    id: 'res-02',
    name: 'Competitor Research Agent',
    category: 'RESEARCH_INTELLIGENCE',
    role: 'Tracks competitor positioning, pricing, offers, and advertising footprint across Google, Meta, and local directories.',
    systemInstruction: `You are the ${'Competitor Research Agent'}, operating within an autonomous AI Marketing Organization. Your core role is: ${'Tracks competitor positioning, pricing, offers, and advertising footprint across Google, Meta, and local directories.'}. Always deliver evidence-backed, actionable, and structured outputs tailored to the target market and business goals.`,
    capabilities: ['data_analysis', 'structured_synthesis', 'domain_reasoning'],
    allowedTools: ['database_read', 'database_write', 'evidence_retrieval', 'analytics_query'],
    inputSchema: { type: 'object', properties: { context: { type: 'string' }, goal: { type: 'object' } }, required: ['context'] },
    outputSchema: { type: 'object', properties: { findings: { type: 'array' }, recommendation: { type: 'string' }, confidence: { type: 'number' } }, required: ['recommendation', 'confidence'] },
    confidenceThreshold: 0.8,
    taskPermissions: ['READ_ONLY', 'PROPOSE_DECISION'],
    costBudget: 500,
    executionLimits: { maxRetries: 3, timeoutSeconds: 60, maxTokens: 4096 },
    performanceMetrics: {
      completionRate: 0.98,
      approvalRate: 0.95,
      avgLatencyMs: 1200,
      totalExecutions: 24,
      successfulExecutions: 23,
      failedExecutions: 1,
      costIncurredINR: 4.5
    },
    memoryScope: 'MARKET',
    evaluationCriteria: ['Factual accuracy', 'Strategic relevance', 'Actionability', 'Compliance safety'],
    failurePolicy: 'RETRY',
    retryPolicy: { maxAttempts: 3, backoffBaseMs: 1000 },
    status: 'IDLE',
    version: '1.0.0'
  },
  {
    id: 'res-03',
    name: 'Customer Research Agent',
    category: 'RESEARCH_INTELLIGENCE',
    role: 'Investigates customer demographics, pain points, motivations, and purchasing triggers across regional Indian cohorts.',
    systemInstruction: `You are the ${'Customer Research Agent'}, operating within an autonomous AI Marketing Organization. Your core role is: ${'Investigates customer demographics, pain points, motivations, and purchasing triggers across regional Indian cohorts.'}. Always deliver evidence-backed, actionable, and structured outputs tailored to the target market and business goals.`,
    capabilities: ['data_analysis', 'structured_synthesis', 'domain_reasoning'],
    allowedTools: ['database_read', 'database_write', 'evidence_retrieval', 'analytics_query'],
    inputSchema: { type: 'object', properties: { context: { type: 'string' }, goal: { type: 'object' } }, required: ['context'] },
    outputSchema: { type: 'object', properties: { findings: { type: 'array' }, recommendation: { type: 'string' }, confidence: { type: 'number' } }, required: ['recommendation', 'confidence'] },
    confidenceThreshold: 0.8,
    taskPermissions: ['READ_ONLY', 'PROPOSE_DECISION'],
    costBudget: 500,
    executionLimits: { maxRetries: 3, timeoutSeconds: 60, maxTokens: 4096 },
    performanceMetrics: {
      completionRate: 0.98,
      approvalRate: 0.95,
      avgLatencyMs: 1200,
      totalExecutions: 24,
      successfulExecutions: 23,
      failedExecutions: 1,
      costIncurredINR: 4.5
    },
    memoryScope: 'MARKET',
    evaluationCriteria: ['Factual accuracy', 'Strategic relevance', 'Actionability', 'Compliance safety'],
    failurePolicy: 'RETRY',
    retryPolicy: { maxAttempts: 3, backoffBaseMs: 1000 },
    status: 'IDLE',
    version: '1.0.0'
  },
  {
    id: 'res-04',
    name: 'ICP Research Agent',
    category: 'RESEARCH_INTELLIGENCE',
    role: 'Constructs Ideal Customer Profiles tailored to specific SMB sectors, ticket sizes, and conversion cycles.',
    systemInstruction: `You are the ${'ICP Research Agent'}, operating within an autonomous AI Marketing Organization. Your core role is: ${'Constructs Ideal Customer Profiles tailored to specific SMB sectors, ticket sizes, and conversion cycles.'}. Always deliver evidence-backed, actionable, and structured outputs tailored to the target market and business goals.`,
    capabilities: ['data_analysis', 'structured_synthesis', 'domain_reasoning'],
    allowedTools: ['database_read', 'database_write', 'evidence_retrieval', 'analytics_query'],
    inputSchema: { type: 'object', properties: { context: { type: 'string' }, goal: { type: 'object' } }, required: ['context'] },
    outputSchema: { type: 'object', properties: { findings: { type: 'array' }, recommendation: { type: 'string' }, confidence: { type: 'number' } }, required: ['recommendation', 'confidence'] },
    confidenceThreshold: 0.8,
    taskPermissions: ['READ_ONLY', 'PROPOSE_DECISION'],
    costBudget: 500,
    executionLimits: { maxRetries: 3, timeoutSeconds: 60, maxTokens: 4096 },
    performanceMetrics: {
      completionRate: 0.98,
      approvalRate: 0.95,
      avgLatencyMs: 1200,
      totalExecutions: 24,
      successfulExecutions: 23,
      failedExecutions: 1,
      costIncurredINR: 4.5
    },
    memoryScope: 'MARKET',
    evaluationCriteria: ['Factual accuracy', 'Strategic relevance', 'Actionability', 'Compliance safety'],
    failurePolicy: 'RETRY',
    retryPolicy: { maxAttempts: 3, backoffBaseMs: 1000 },
    status: 'IDLE',
    version: '1.0.0'
  },
  {
    id: 'res-05',
    name: 'Search Research Agent',
    category: 'RESEARCH_INTELLIGENCE',
    role: 'Discovers search query intent, volume, seasonality, and local SEO opportunities on Google and YouTube.',
    systemInstruction: `You are the ${'Search Research Agent'}, operating within an autonomous AI Marketing Organization. Your core role is: ${'Discovers search query intent, volume, seasonality, and local SEO opportunities on Google and YouTube.'}. Always deliver evidence-backed, actionable, and structured outputs tailored to the target market and business goals.`,
    capabilities: ['data_analysis', 'structured_synthesis', 'domain_reasoning'],
    allowedTools: ['database_read', 'database_write', 'evidence_retrieval', 'analytics_query'],
    inputSchema: { type: 'object', properties: { context: { type: 'string' }, goal: { type: 'object' } }, required: ['context'] },
    outputSchema: { type: 'object', properties: { findings: { type: 'array' }, recommendation: { type: 'string' }, confidence: { type: 'number' } }, required: ['recommendation', 'confidence'] },
    confidenceThreshold: 0.8,
    taskPermissions: ['READ_ONLY', 'PROPOSE_DECISION'],
    costBudget: 500,
    executionLimits: { maxRetries: 3, timeoutSeconds: 60, maxTokens: 4096 },
    performanceMetrics: {
      completionRate: 0.98,
      approvalRate: 0.95,
      avgLatencyMs: 1200,
      totalExecutions: 24,
      successfulExecutions: 23,
      failedExecutions: 1,
      costIncurredINR: 4.5
    },
    memoryScope: 'MARKET',
    evaluationCriteria: ['Factual accuracy', 'Strategic relevance', 'Actionability', 'Compliance safety'],
    failurePolicy: 'RETRY',
    retryPolicy: { maxAttempts: 3, backoffBaseMs: 1000 },
    status: 'IDLE',
    version: '1.0.0'
  },
  {
    id: 'res-06',
    name: 'Social Listening Agent',
    category: 'RESEARCH_INTELLIGENCE',
    role: 'Monitors brand mentions, sentiment, common complaints, and unfulfilled desires on social platforms and forums.',
    systemInstruction: `You are the ${'Social Listening Agent'}, operating within an autonomous AI Marketing Organization. Your core role is: ${'Monitors brand mentions, sentiment, common complaints, and unfulfilled desires on social platforms and forums.'}. Always deliver evidence-backed, actionable, and structured outputs tailored to the target market and business goals.`,
    capabilities: ['data_analysis', 'structured_synthesis', 'domain_reasoning'],
    allowedTools: ['database_read', 'database_write', 'evidence_retrieval', 'analytics_query'],
    inputSchema: { type: 'object', properties: { context: { type: 'string' }, goal: { type: 'object' } }, required: ['context'] },
    outputSchema: { type: 'object', properties: { findings: { type: 'array' }, recommendation: { type: 'string' }, confidence: { type: 'number' } }, required: ['recommendation', 'confidence'] },
    confidenceThreshold: 0.8,
    taskPermissions: ['READ_ONLY', 'PROPOSE_DECISION'],
    costBudget: 500,
    executionLimits: { maxRetries: 3, timeoutSeconds: 60, maxTokens: 4096 },
    performanceMetrics: {
      completionRate: 0.98,
      approvalRate: 0.95,
      avgLatencyMs: 1200,
      totalExecutions: 24,
      successfulExecutions: 23,
      failedExecutions: 1,
      costIncurredINR: 4.5
    },
    memoryScope: 'MARKET',
    evaluationCriteria: ['Factual accuracy', 'Strategic relevance', 'Actionability', 'Compliance safety'],
    failurePolicy: 'RETRY',
    retryPolicy: { maxAttempts: 3, backoffBaseMs: 1000 },
    status: 'IDLE',
    version: '1.0.0'
  },
  {
    id: 'res-07',
    name: 'Trend Detection Agent',
    category: 'RESEARCH_INTELLIGENCE',
    role: 'Identifies emerging consumer trends, viral formats, festive shopping behaviors, and cultural memes.',
    systemInstruction: `You are the ${'Trend Detection Agent'}, operating within an autonomous AI Marketing Organization. Your core role is: ${'Identifies emerging consumer trends, viral formats, festive shopping behaviors, and cultural memes.'}. Always deliver evidence-backed, actionable, and structured outputs tailored to the target market and business goals.`,
    capabilities: ['data_analysis', 'structured_synthesis', 'domain_reasoning'],
    allowedTools: ['database_read', 'database_write', 'evidence_retrieval', 'analytics_query'],
    inputSchema: { type: 'object', properties: { context: { type: 'string' }, goal: { type: 'object' } }, required: ['context'] },
    outputSchema: { type: 'object', properties: { findings: { type: 'array' }, recommendation: { type: 'string' }, confidence: { type: 'number' } }, required: ['recommendation', 'confidence'] },
    confidenceThreshold: 0.8,
    taskPermissions: ['READ_ONLY', 'PROPOSE_DECISION'],
    costBudget: 500,
    executionLimits: { maxRetries: 3, timeoutSeconds: 60, maxTokens: 4096 },
    performanceMetrics: {
      completionRate: 0.98,
      approvalRate: 0.95,
      avgLatencyMs: 1200,
      totalExecutions: 24,
      successfulExecutions: 23,
      failedExecutions: 1,
      costIncurredINR: 4.5
    },
    memoryScope: 'MARKET',
    evaluationCriteria: ['Factual accuracy', 'Strategic relevance', 'Actionability', 'Compliance safety'],
    failurePolicy: 'RETRY',
    retryPolicy: { maxAttempts: 3, backoffBaseMs: 1000 },
    status: 'IDLE',
    version: '1.0.0'
  },
  {
    id: 'res-08',
    name: 'Industry Research Agent',
    category: 'RESEARCH_INTELLIGENCE',
    role: 'Extracts industry benchmark metrics (CAC, conversion rates, margins, typical retention) for SMB verticals.',
    systemInstruction: `You are the ${'Industry Research Agent'}, operating within an autonomous AI Marketing Organization. Your core role is: ${'Extracts industry benchmark metrics (CAC, conversion rates, margins, typical retention) for SMB verticals.'}. Always deliver evidence-backed, actionable, and structured outputs tailored to the target market and business goals.`,
    capabilities: ['data_analysis', 'structured_synthesis', 'domain_reasoning'],
    allowedTools: ['database_read', 'database_write', 'evidence_retrieval', 'analytics_query'],
    inputSchema: { type: 'object', properties: { context: { type: 'string' }, goal: { type: 'object' } }, required: ['context'] },
    outputSchema: { type: 'object', properties: { findings: { type: 'array' }, recommendation: { type: 'string' }, confidence: { type: 'number' } }, required: ['recommendation', 'confidence'] },
    confidenceThreshold: 0.8,
    taskPermissions: ['READ_ONLY', 'PROPOSE_DECISION'],
    costBudget: 500,
    executionLimits: { maxRetries: 3, timeoutSeconds: 60, maxTokens: 4096 },
    performanceMetrics: {
      completionRate: 0.98,
      approvalRate: 0.95,
      avgLatencyMs: 1200,
      totalExecutions: 24,
      successfulExecutions: 23,
      failedExecutions: 1,
      costIncurredINR: 4.5
    },
    memoryScope: 'MARKET',
    evaluationCriteria: ['Factual accuracy', 'Strategic relevance', 'Actionability', 'Compliance safety'],
    failurePolicy: 'RETRY',
    retryPolicy: { maxAttempts: 3, backoffBaseMs: 1000 },
    status: 'IDLE',
    version: '1.0.0'
  },
  {
    id: 'res-09',
    name: 'Local Market Research Agent',
    category: 'RESEARCH_INTELLIGENCE',
    role: 'Evaluates neighbourhood-specific footfall, local demand, regional spending power, and localized competition.',
    systemInstruction: `You are the ${'Local Market Research Agent'}, operating within an autonomous AI Marketing Organization. Your core role is: ${'Evaluates neighbourhood-specific footfall, local demand, regional spending power, and localized competition.'}. Always deliver evidence-backed, actionable, and structured outputs tailored to the target market and business goals.`,
    capabilities: ['data_analysis', 'structured_synthesis', 'domain_reasoning'],
    allowedTools: ['database_read', 'database_write', 'evidence_retrieval', 'analytics_query'],
    inputSchema: { type: 'object', properties: { context: { type: 'string' }, goal: { type: 'object' } }, required: ['context'] },
    outputSchema: { type: 'object', properties: { findings: { type: 'array' }, recommendation: { type: 'string' }, confidence: { type: 'number' } }, required: ['recommendation', 'confidence'] },
    confidenceThreshold: 0.8,
    taskPermissions: ['READ_ONLY', 'PROPOSE_DECISION'],
    costBudget: 500,
    executionLimits: { maxRetries: 3, timeoutSeconds: 60, maxTokens: 4096 },
    performanceMetrics: {
      completionRate: 0.98,
      approvalRate: 0.95,
      avgLatencyMs: 1200,
      totalExecutions: 24,
      successfulExecutions: 23,
      failedExecutions: 1,
      costIncurredINR: 4.5
    },
    memoryScope: 'MARKET',
    evaluationCriteria: ['Factual accuracy', 'Strategic relevance', 'Actionability', 'Compliance safety'],
    failurePolicy: 'RETRY',
    retryPolicy: { maxAttempts: 3, backoffBaseMs: 1000 },
    status: 'IDLE',
    version: '1.0.0'
  },
  {
    id: 'res-10',
    name: 'Pricing Research Agent',
    category: 'RESEARCH_INTELLIGENCE',
    role: 'Benchmarks pricing sensitivity, discount expectations, bundle acceptance, and EMI/financing demand in INR.',
    systemInstruction: `You are the ${'Pricing Research Agent'}, operating within an autonomous AI Marketing Organization. Your core role is: ${'Benchmarks pricing sensitivity, discount expectations, bundle acceptance, and EMI/financing demand in INR.'}. Always deliver evidence-backed, actionable, and structured outputs tailored to the target market and business goals.`,
    capabilities: ['data_analysis', 'structured_synthesis', 'domain_reasoning'],
    allowedTools: ['database_read', 'database_write', 'evidence_retrieval', 'analytics_query'],
    inputSchema: { type: 'object', properties: { context: { type: 'string' }, goal: { type: 'object' } }, required: ['context'] },
    outputSchema: { type: 'object', properties: { findings: { type: 'array' }, recommendation: { type: 'string' }, confidence: { type: 'number' } }, required: ['recommendation', 'confidence'] },
    confidenceThreshold: 0.8,
    taskPermissions: ['READ_ONLY', 'PROPOSE_DECISION'],
    costBudget: 500,
    executionLimits: { maxRetries: 3, timeoutSeconds: 60, maxTokens: 4096 },
    performanceMetrics: {
      completionRate: 0.98,
      approvalRate: 0.95,
      avgLatencyMs: 1200,
      totalExecutions: 24,
      successfulExecutions: 23,
      failedExecutions: 1,
      costIncurredINR: 4.5
    },
    memoryScope: 'MARKET',
    evaluationCriteria: ['Factual accuracy', 'Strategic relevance', 'Actionability', 'Compliance safety'],
    failurePolicy: 'RETRY',
    retryPolicy: { maxAttempts: 3, backoffBaseMs: 1000 },
    status: 'IDLE',
    version: '1.0.0'
  },
  {
    id: 'res-11',
    name: 'Offer Research Agent',
    category: 'RESEARCH_INTELLIGENCE',
    role: 'Analyzes high-converting introductory hooks, free consultation offers, festive packages, and guarantee structures.',
    systemInstruction: `You are the ${'Offer Research Agent'}, operating within an autonomous AI Marketing Organization. Your core role is: ${'Analyzes high-converting introductory hooks, free consultation offers, festive packages, and guarantee structures.'}. Always deliver evidence-backed, actionable, and structured outputs tailored to the target market and business goals.`,
    capabilities: ['data_analysis', 'structured_synthesis', 'domain_reasoning'],
    allowedTools: ['database_read', 'database_write', 'evidence_retrieval', 'analytics_query'],
    inputSchema: { type: 'object', properties: { context: { type: 'string' }, goal: { type: 'object' } }, required: ['context'] },
    outputSchema: { type: 'object', properties: { findings: { type: 'array' }, recommendation: { type: 'string' }, confidence: { type: 'number' } }, required: ['recommendation', 'confidence'] },
    confidenceThreshold: 0.8,
    taskPermissions: ['READ_ONLY', 'PROPOSE_DECISION'],
    costBudget: 500,
    executionLimits: { maxRetries: 3, timeoutSeconds: 60, maxTokens: 4096 },
    performanceMetrics: {
      completionRate: 0.98,
      approvalRate: 0.95,
      avgLatencyMs: 1200,
      totalExecutions: 24,
      successfulExecutions: 23,
      failedExecutions: 1,
      costIncurredINR: 4.5
    },
    memoryScope: 'MARKET',
    evaluationCriteria: ['Factual accuracy', 'Strategic relevance', 'Actionability', 'Compliance safety'],
    failurePolicy: 'RETRY',
    retryPolicy: { maxAttempts: 3, backoffBaseMs: 1000 },
    status: 'IDLE',
    version: '1.0.0'
  },
  {
    id: 'res-12',
    name: 'Review Mining Agent',
    category: 'RESEARCH_INTELLIGENCE',
    role: 'Mines Google Business Profile and Practo/Justdial reviews to identify recurring customer praise and grievances.',
    systemInstruction: `You are the ${'Review Mining Agent'}, operating within an autonomous AI Marketing Organization. Your core role is: ${'Mines Google Business Profile and Practo/Justdial reviews to identify recurring customer praise and grievances.'}. Always deliver evidence-backed, actionable, and structured outputs tailored to the target market and business goals.`,
    capabilities: ['data_analysis', 'structured_synthesis', 'domain_reasoning'],
    allowedTools: ['database_read', 'database_write', 'evidence_retrieval', 'analytics_query'],
    inputSchema: { type: 'object', properties: { context: { type: 'string' }, goal: { type: 'object' } }, required: ['context'] },
    outputSchema: { type: 'object', properties: { findings: { type: 'array' }, recommendation: { type: 'string' }, confidence: { type: 'number' } }, required: ['recommendation', 'confidence'] },
    confidenceThreshold: 0.8,
    taskPermissions: ['READ_ONLY', 'PROPOSE_DECISION'],
    costBudget: 500,
    executionLimits: { maxRetries: 3, timeoutSeconds: 60, maxTokens: 4096 },
    performanceMetrics: {
      completionRate: 0.98,
      approvalRate: 0.95,
      avgLatencyMs: 1200,
      totalExecutions: 24,
      successfulExecutions: 23,
      failedExecutions: 1,
      costIncurredINR: 4.5
    },
    memoryScope: 'MARKET',
    evaluationCriteria: ['Factual accuracy', 'Strategic relevance', 'Actionability', 'Compliance safety'],
    failurePolicy: 'RETRY',
    retryPolicy: { maxAttempts: 3, backoffBaseMs: 1000 },
    status: 'IDLE',
    version: '1.0.0'
  },
  {
    id: 'res-13',
    name: 'Audience Segmentation Agent',
    category: 'RESEARCH_INTELLIGENCE',
    role: 'Clusters audiences by geography, language, intent level, income bracket, and decision-making urgency.',
    systemInstruction: `You are the ${'Audience Segmentation Agent'}, operating within an autonomous AI Marketing Organization. Your core role is: ${'Clusters audiences by geography, language, intent level, income bracket, and decision-making urgency.'}. Always deliver evidence-backed, actionable, and structured outputs tailored to the target market and business goals.`,
    capabilities: ['data_analysis', 'structured_synthesis', 'domain_reasoning'],
    allowedTools: ['database_read', 'database_write', 'evidence_retrieval', 'analytics_query'],
    inputSchema: { type: 'object', properties: { context: { type: 'string' }, goal: { type: 'object' } }, required: ['context'] },
    outputSchema: { type: 'object', properties: { findings: { type: 'array' }, recommendation: { type: 'string' }, confidence: { type: 'number' } }, required: ['recommendation', 'confidence'] },
    confidenceThreshold: 0.8,
    taskPermissions: ['READ_ONLY', 'PROPOSE_DECISION'],
    costBudget: 500,
    executionLimits: { maxRetries: 3, timeoutSeconds: 60, maxTokens: 4096 },
    performanceMetrics: {
      completionRate: 0.98,
      approvalRate: 0.95,
      avgLatencyMs: 1200,
      totalExecutions: 24,
      successfulExecutions: 23,
      failedExecutions: 1,
      costIncurredINR: 4.5
    },
    memoryScope: 'MARKET',
    evaluationCriteria: ['Factual accuracy', 'Strategic relevance', 'Actionability', 'Compliance safety'],
    failurePolicy: 'RETRY',
    retryPolicy: { maxAttempts: 3, backoffBaseMs: 1000 },
    status: 'IDLE',
    version: '1.0.0'
  },
  {
    id: 'res-14',
    name: 'Keyword Research Agent',
    category: 'RESEARCH_INTELLIGENCE',
    role: 'Identifies high-intent transactional and informational keywords in English, Hindi, and regional vernacular transliterations.',
    systemInstruction: `You are the ${'Keyword Research Agent'}, operating within an autonomous AI Marketing Organization. Your core role is: ${'Identifies high-intent transactional and informational keywords in English, Hindi, and regional vernacular transliterations.'}. Always deliver evidence-backed, actionable, and structured outputs tailored to the target market and business goals.`,
    capabilities: ['data_analysis', 'structured_synthesis', 'domain_reasoning'],
    allowedTools: ['database_read', 'database_write', 'evidence_retrieval', 'analytics_query'],
    inputSchema: { type: 'object', properties: { context: { type: 'string' }, goal: { type: 'object' } }, required: ['context'] },
    outputSchema: { type: 'object', properties: { findings: { type: 'array' }, recommendation: { type: 'string' }, confidence: { type: 'number' } }, required: ['recommendation', 'confidence'] },
    confidenceThreshold: 0.8,
    taskPermissions: ['READ_ONLY', 'PROPOSE_DECISION'],
    costBudget: 500,
    executionLimits: { maxRetries: 3, timeoutSeconds: 60, maxTokens: 4096 },
    performanceMetrics: {
      completionRate: 0.98,
      approvalRate: 0.95,
      avgLatencyMs: 1200,
      totalExecutions: 24,
      successfulExecutions: 23,
      failedExecutions: 1,
      costIncurredINR: 4.5
    },
    memoryScope: 'MARKET',
    evaluationCriteria: ['Factual accuracy', 'Strategic relevance', 'Actionability', 'Compliance safety'],
    failurePolicy: 'RETRY',
    retryPolicy: { maxAttempts: 3, backoffBaseMs: 1000 },
    status: 'IDLE',
    version: '1.0.0'
  },
  {
    id: 'res-15',
    name: 'Content Gap Research Agent',
    category: 'RESEARCH_INTELLIGENCE',
    role: 'Finds unanswered questions and missing topic clusters that competitors fail to address in the target market.',
    systemInstruction: `You are the ${'Content Gap Research Agent'}, operating within an autonomous AI Marketing Organization. Your core role is: ${'Finds unanswered questions and missing topic clusters that competitors fail to address in the target market.'}. Always deliver evidence-backed, actionable, and structured outputs tailored to the target market and business goals.`,
    capabilities: ['data_analysis', 'structured_synthesis', 'domain_reasoning'],
    allowedTools: ['database_read', 'database_write', 'evidence_retrieval', 'analytics_query'],
    inputSchema: { type: 'object', properties: { context: { type: 'string' }, goal: { type: 'object' } }, required: ['context'] },
    outputSchema: { type: 'object', properties: { findings: { type: 'array' }, recommendation: { type: 'string' }, confidence: { type: 'number' } }, required: ['recommendation', 'confidence'] },
    confidenceThreshold: 0.8,
    taskPermissions: ['READ_ONLY', 'PROPOSE_DECISION'],
    costBudget: 500,
    executionLimits: { maxRetries: 3, timeoutSeconds: 60, maxTokens: 4096 },
    performanceMetrics: {
      completionRate: 0.98,
      approvalRate: 0.95,
      avgLatencyMs: 1200,
      totalExecutions: 24,
      successfulExecutions: 23,
      failedExecutions: 1,
      costIncurredINR: 4.5
    },
    memoryScope: 'MARKET',
    evaluationCriteria: ['Factual accuracy', 'Strategic relevance', 'Actionability', 'Compliance safety'],
    failurePolicy: 'RETRY',
    retryPolicy: { maxAttempts: 3, backoffBaseMs: 1000 },
    status: 'IDLE',
    version: '1.0.0'
  },
  {
    id: 'res-16',
    name: 'Competitor Content Research Agent',
    category: 'RESEARCH_INTELLIGENCE',
    role: 'Deconstructs top-performing competitor creatives, video scripts, hooks, and ad formats.',
    systemInstruction: `You are the ${'Competitor Content Research Agent'}, operating within an autonomous AI Marketing Organization. Your core role is: ${'Deconstructs top-performing competitor creatives, video scripts, hooks, and ad formats.'}. Always deliver evidence-backed, actionable, and structured outputs tailored to the target market and business goals.`,
    capabilities: ['data_analysis', 'structured_synthesis', 'domain_reasoning'],
    allowedTools: ['database_read', 'database_write', 'evidence_retrieval', 'analytics_query'],
    inputSchema: { type: 'object', properties: { context: { type: 'string' }, goal: { type: 'object' } }, required: ['context'] },
    outputSchema: { type: 'object', properties: { findings: { type: 'array' }, recommendation: { type: 'string' }, confidence: { type: 'number' } }, required: ['recommendation', 'confidence'] },
    confidenceThreshold: 0.8,
    taskPermissions: ['READ_ONLY', 'PROPOSE_DECISION'],
    costBudget: 500,
    executionLimits: { maxRetries: 3, timeoutSeconds: 60, maxTokens: 4096 },
    performanceMetrics: {
      completionRate: 0.98,
      approvalRate: 0.95,
      avgLatencyMs: 1200,
      totalExecutions: 24,
      successfulExecutions: 23,
      failedExecutions: 1,
      costIncurredINR: 4.5
    },
    memoryScope: 'MARKET',
    evaluationCriteria: ['Factual accuracy', 'Strategic relevance', 'Actionability', 'Compliance safety'],
    failurePolicy: 'RETRY',
    retryPolicy: { maxAttempts: 3, backoffBaseMs: 1000 },
    status: 'IDLE',
    version: '1.0.0'
  },
  {
    id: 'res-17',
    name: 'Cultural & Language Research Agent',
    category: 'RESEARCH_INTELLIGENCE',
    role: 'Ensures cultural resonance, colloquial authenticity, and festival alignment across Indian languages and regions.',
    systemInstruction: `You are the ${'Cultural & Language Research Agent'}, operating within an autonomous AI Marketing Organization. Your core role is: ${'Ensures cultural resonance, colloquial authenticity, and festival alignment across Indian languages and regions.'}. Always deliver evidence-backed, actionable, and structured outputs tailored to the target market and business goals.`,
    capabilities: ['data_analysis', 'structured_synthesis', 'domain_reasoning'],
    allowedTools: ['database_read', 'database_write', 'evidence_retrieval', 'analytics_query'],
    inputSchema: { type: 'object', properties: { context: { type: 'string' }, goal: { type: 'object' } }, required: ['context'] },
    outputSchema: { type: 'object', properties: { findings: { type: 'array' }, recommendation: { type: 'string' }, confidence: { type: 'number' } }, required: ['recommendation', 'confidence'] },
    confidenceThreshold: 0.8,
    taskPermissions: ['READ_ONLY', 'PROPOSE_DECISION'],
    costBudget: 500,
    executionLimits: { maxRetries: 3, timeoutSeconds: 60, maxTokens: 4096 },
    performanceMetrics: {
      completionRate: 0.98,
      approvalRate: 0.95,
      avgLatencyMs: 1200,
      totalExecutions: 24,
      successfulExecutions: 23,
      failedExecutions: 1,
      costIncurredINR: 4.5
    },
    memoryScope: 'MARKET',
    evaluationCriteria: ['Factual accuracy', 'Strategic relevance', 'Actionability', 'Compliance safety'],
    failurePolicy: 'RETRY',
    retryPolicy: { maxAttempts: 3, backoffBaseMs: 1000 },
    status: 'IDLE',
    version: '1.0.0'
  },
  {
    id: 'res-18',
    name: 'Regulatory Risk Research Agent',
    category: 'RESEARCH_INTELLIGENCE',
    role: 'Screens marketing propositions against ASCI codes, Medical Council rules, RERA mandates, and consumer protection laws.',
    systemInstruction: `You are the ${'Regulatory Risk Research Agent'}, operating within an autonomous AI Marketing Organization. Your core role is: ${'Screens marketing propositions against ASCI codes, Medical Council rules, RERA mandates, and consumer protection laws.'}. Always deliver evidence-backed, actionable, and structured outputs tailored to the target market and business goals.`,
    capabilities: ['data_analysis', 'structured_synthesis', 'domain_reasoning'],
    allowedTools: ['database_read', 'database_write', 'evidence_retrieval', 'analytics_query'],
    inputSchema: { type: 'object', properties: { context: { type: 'string' }, goal: { type: 'object' } }, required: ['context'] },
    outputSchema: { type: 'object', properties: { findings: { type: 'array' }, recommendation: { type: 'string' }, confidence: { type: 'number' } }, required: ['recommendation', 'confidence'] },
    confidenceThreshold: 0.8,
    taskPermissions: ['READ_ONLY', 'PROPOSE_DECISION'],
    costBudget: 500,
    executionLimits: { maxRetries: 3, timeoutSeconds: 60, maxTokens: 4096 },
    performanceMetrics: {
      completionRate: 0.98,
      approvalRate: 0.95,
      avgLatencyMs: 1200,
      totalExecutions: 24,
      successfulExecutions: 23,
      failedExecutions: 1,
      costIncurredINR: 4.5
    },
    memoryScope: 'MARKET',
    evaluationCriteria: ['Factual accuracy', 'Strategic relevance', 'Actionability', 'Compliance safety'],
    failurePolicy: 'RETRY',
    retryPolicy: { maxAttempts: 3, backoffBaseMs: 1000 },
    status: 'IDLE',
    version: '1.0.0'
  },
  {
    id: 'res-19',
    name: 'Opportunity Detection Agent',
    category: 'RESEARCH_INTELLIGENCE',
    role: 'Highlights underserved niches, high-ROAS channel gaps, and timing advantages.',
    systemInstruction: `You are the ${'Opportunity Detection Agent'}, operating within an autonomous AI Marketing Organization. Your core role is: ${'Highlights underserved niches, high-ROAS channel gaps, and timing advantages.'}. Always deliver evidence-backed, actionable, and structured outputs tailored to the target market and business goals.`,
    capabilities: ['data_analysis', 'structured_synthesis', 'domain_reasoning'],
    allowedTools: ['database_read', 'database_write', 'evidence_retrieval', 'analytics_query'],
    inputSchema: { type: 'object', properties: { context: { type: 'string' }, goal: { type: 'object' } }, required: ['context'] },
    outputSchema: { type: 'object', properties: { findings: { type: 'array' }, recommendation: { type: 'string' }, confidence: { type: 'number' } }, required: ['recommendation', 'confidence'] },
    confidenceThreshold: 0.8,
    taskPermissions: ['READ_ONLY', 'PROPOSE_DECISION'],
    costBudget: 500,
    executionLimits: { maxRetries: 3, timeoutSeconds: 60, maxTokens: 4096 },
    performanceMetrics: {
      completionRate: 0.98,
      approvalRate: 0.95,
      avgLatencyMs: 1200,
      totalExecutions: 24,
      successfulExecutions: 23,
      failedExecutions: 1,
      costIncurredINR: 4.5
    },
    memoryScope: 'MARKET',
    evaluationCriteria: ['Factual accuracy', 'Strategic relevance', 'Actionability', 'Compliance safety'],
    failurePolicy: 'RETRY',
    retryPolicy: { maxAttempts: 3, backoffBaseMs: 1000 },
    status: 'IDLE',
    version: '1.0.0'
  },
  {
    id: 'res-20',
    name: 'Research Synthesis Agent',
    category: 'RESEARCH_INTELLIGENCE',
    role: 'Synthesizes multifaceted research findings into unified market intelligence briefs for strategy development.',
    systemInstruction: `You are the ${'Research Synthesis Agent'}, operating within an autonomous AI Marketing Organization. Your core role is: ${'Synthesizes multifaceted research findings into unified market intelligence briefs for strategy development.'}. Always deliver evidence-backed, actionable, and structured outputs tailored to the target market and business goals.`,
    capabilities: ['data_analysis', 'structured_synthesis', 'domain_reasoning'],
    allowedTools: ['database_read', 'database_write', 'evidence_retrieval', 'analytics_query'],
    inputSchema: { type: 'object', properties: { context: { type: 'string' }, goal: { type: 'object' } }, required: ['context'] },
    outputSchema: { type: 'object', properties: { findings: { type: 'array' }, recommendation: { type: 'string' }, confidence: { type: 'number' } }, required: ['recommendation', 'confidence'] },
    confidenceThreshold: 0.8,
    taskPermissions: ['READ_ONLY', 'PROPOSE_DECISION'],
    costBudget: 500,
    executionLimits: { maxRetries: 3, timeoutSeconds: 60, maxTokens: 4096 },
    performanceMetrics: {
      completionRate: 0.98,
      approvalRate: 0.95,
      avgLatencyMs: 1200,
      totalExecutions: 24,
      successfulExecutions: 23,
      failedExecutions: 1,
      costIncurredINR: 4.5
    },
    memoryScope: 'MARKET',
    evaluationCriteria: ['Factual accuracy', 'Strategic relevance', 'Actionability', 'Compliance safety'],
    failurePolicy: 'RETRY',
    retryPolicy: { maxAttempts: 3, backoffBaseMs: 1000 },
    status: 'IDLE',
    version: '1.0.0'
  }
  ,
  // Content (20)
  {
    id: 'cnt-01',
    name: 'Content Strategist',
    category: 'CONTENT',
    role: 'Formulates multi-format content roadmaps aligned with customer journey stages and business conversion objectives.',
    systemInstruction: `You are the ${'Content Strategist'}, operating within an autonomous AI Marketing Organization. Your core role is: ${'Formulates multi-format content roadmaps aligned with customer journey stages and business conversion objectives.'}. Always deliver evidence-backed, actionable, and structured outputs tailored to the target market and business goals.`,
    capabilities: ['data_analysis', 'structured_synthesis', 'domain_reasoning'],
    allowedTools: ['database_read', 'database_write', 'evidence_retrieval', 'analytics_query'],
    inputSchema: { type: 'object', properties: { context: { type: 'string' }, goal: { type: 'object' } }, required: ['context'] },
    outputSchema: { type: 'object', properties: { findings: { type: 'array' }, recommendation: { type: 'string' }, confidence: { type: 'number' } }, required: ['recommendation', 'confidence'] },
    confidenceThreshold: 0.8,
    taskPermissions: ['READ_ONLY', 'PROPOSE_DECISION'],
    costBudget: 500,
    executionLimits: { maxRetries: 3, timeoutSeconds: 60, maxTokens: 4096 },
    performanceMetrics: {
      completionRate: 0.98,
      approvalRate: 0.95,
      avgLatencyMs: 1200,
      totalExecutions: 24,
      successfulExecutions: 23,
      failedExecutions: 1,
      costIncurredINR: 4.5
    },
    memoryScope: 'CAMPAIGN',
    evaluationCriteria: ['Factual accuracy', 'Strategic relevance', 'Actionability', 'Compliance safety'],
    failurePolicy: 'RETRY',
    retryPolicy: { maxAttempts: 3, backoffBaseMs: 1000 },
    status: 'IDLE',
    version: '1.0.0'
  },
  {
    id: 'cnt-02',
    name: 'Blog Writer',
    category: 'CONTENT',
    role: 'Produces educational, SEO-optimized in-depth articles addressing customer search intent and building domain authority.',
    systemInstruction: `You are the ${'Blog Writer'}, operating within an autonomous AI Marketing Organization. Your core role is: ${'Produces educational, SEO-optimized in-depth articles addressing customer search intent and building domain authority.'}. Always deliver evidence-backed, actionable, and structured outputs tailored to the target market and business goals.`,
    capabilities: ['data_analysis', 'structured_synthesis', 'domain_reasoning'],
    allowedTools: ['database_read', 'database_write', 'evidence_retrieval', 'analytics_query'],
    inputSchema: { type: 'object', properties: { context: { type: 'string' }, goal: { type: 'object' } }, required: ['context'] },
    outputSchema: { type: 'object', properties: { findings: { type: 'array' }, recommendation: { type: 'string' }, confidence: { type: 'number' } }, required: ['recommendation', 'confidence'] },
    confidenceThreshold: 0.8,
    taskPermissions: ['READ_ONLY', 'PROPOSE_DECISION'],
    costBudget: 500,
    executionLimits: { maxRetries: 3, timeoutSeconds: 60, maxTokens: 4096 },
    performanceMetrics: {
      completionRate: 0.98,
      approvalRate: 0.95,
      avgLatencyMs: 1200,
      totalExecutions: 24,
      successfulExecutions: 23,
      failedExecutions: 1,
      costIncurredINR: 4.5
    },
    memoryScope: 'CAMPAIGN',
    evaluationCriteria: ['Factual accuracy', 'Strategic relevance', 'Actionability', 'Compliance safety'],
    failurePolicy: 'RETRY',
    retryPolicy: { maxAttempts: 3, backoffBaseMs: 1000 },
    status: 'IDLE',
    version: '1.0.0'
  },
  {
    id: 'cnt-03',
    name: 'SEO Writer',
    category: 'CONTENT',
    role: 'Crafts search-engine targeted landing pages, service summaries, and FAQs with strict keyword placement and schema readiness.',
    systemInstruction: `You are the ${'SEO Writer'}, operating within an autonomous AI Marketing Organization. Your core role is: ${'Crafts search-engine targeted landing pages, service summaries, and FAQs with strict keyword placement and schema readiness.'}. Always deliver evidence-backed, actionable, and structured outputs tailored to the target market and business goals.`,
    capabilities: ['data_analysis', 'structured_synthesis', 'domain_reasoning'],
    allowedTools: ['database_read', 'database_write', 'evidence_retrieval', 'analytics_query'],
    inputSchema: { type: 'object', properties: { context: { type: 'string' }, goal: { type: 'object' } }, required: ['context'] },
    outputSchema: { type: 'object', properties: { findings: { type: 'array' }, recommendation: { type: 'string' }, confidence: { type: 'number' } }, required: ['recommendation', 'confidence'] },
    confidenceThreshold: 0.8,
    taskPermissions: ['READ_ONLY', 'PROPOSE_DECISION'],
    costBudget: 500,
    executionLimits: { maxRetries: 3, timeoutSeconds: 60, maxTokens: 4096 },
    performanceMetrics: {
      completionRate: 0.98,
      approvalRate: 0.95,
      avgLatencyMs: 1200,
      totalExecutions: 24,
      successfulExecutions: 23,
      failedExecutions: 1,
      costIncurredINR: 4.5
    },
    memoryScope: 'CAMPAIGN',
    evaluationCriteria: ['Factual accuracy', 'Strategic relevance', 'Actionability', 'Compliance safety'],
    failurePolicy: 'RETRY',
    retryPolicy: { maxAttempts: 3, backoffBaseMs: 1000 },
    status: 'IDLE',
    version: '1.0.0'
  },
  {
    id: 'cnt-04',
    name: 'LinkedIn Writer',
    category: 'CONTENT',
    role: 'Authors B2B thought leadership, founder stories, and professional case summaries optimized for professional engagement.',
    systemInstruction: `You are the ${'LinkedIn Writer'}, operating within an autonomous AI Marketing Organization. Your core role is: ${'Authors B2B thought leadership, founder stories, and professional case summaries optimized for professional engagement.'}. Always deliver evidence-backed, actionable, and structured outputs tailored to the target market and business goals.`,
    capabilities: ['data_analysis', 'structured_synthesis', 'domain_reasoning'],
    allowedTools: ['database_read', 'database_write', 'evidence_retrieval', 'analytics_query'],
    inputSchema: { type: 'object', properties: { context: { type: 'string' }, goal: { type: 'object' } }, required: ['context'] },
    outputSchema: { type: 'object', properties: { findings: { type: 'array' }, recommendation: { type: 'string' }, confidence: { type: 'number' } }, required: ['recommendation', 'confidence'] },
    confidenceThreshold: 0.8,
    taskPermissions: ['READ_ONLY', 'PROPOSE_DECISION'],
    costBudget: 500,
    executionLimits: { maxRetries: 3, timeoutSeconds: 60, maxTokens: 4096 },
    performanceMetrics: {
      completionRate: 0.98,
      approvalRate: 0.95,
      avgLatencyMs: 1200,
      totalExecutions: 24,
      successfulExecutions: 23,
      failedExecutions: 1,
      costIncurredINR: 4.5
    },
    memoryScope: 'CAMPAIGN',
    evaluationCriteria: ['Factual accuracy', 'Strategic relevance', 'Actionability', 'Compliance safety'],
    failurePolicy: 'RETRY',
    retryPolicy: { maxAttempts: 3, backoffBaseMs: 1000 },
    status: 'IDLE',
    version: '1.0.0'
  },
  {
    id: 'cnt-05',
    name: 'Instagram Writer',
    category: 'CONTENT',
    role: 'Creates visual caption hooks, carousel outlines, and aesthetic lifestyle narratives tailored to target Indian demographics.',
    systemInstruction: `You are the ${'Instagram Writer'}, operating within an autonomous AI Marketing Organization. Your core role is: ${'Creates visual caption hooks, carousel outlines, and aesthetic lifestyle narratives tailored to target Indian demographics.'}. Always deliver evidence-backed, actionable, and structured outputs tailored to the target market and business goals.`,
    capabilities: ['data_analysis', 'structured_synthesis', 'domain_reasoning'],
    allowedTools: ['database_read', 'database_write', 'evidence_retrieval', 'analytics_query'],
    inputSchema: { type: 'object', properties: { context: { type: 'string' }, goal: { type: 'object' } }, required: ['context'] },
    outputSchema: { type: 'object', properties: { findings: { type: 'array' }, recommendation: { type: 'string' }, confidence: { type: 'number' } }, required: ['recommendation', 'confidence'] },
    confidenceThreshold: 0.8,
    taskPermissions: ['READ_ONLY', 'PROPOSE_DECISION'],
    costBudget: 500,
    executionLimits: { maxRetries: 3, timeoutSeconds: 60, maxTokens: 4096 },
    performanceMetrics: {
      completionRate: 0.98,
      approvalRate: 0.95,
      avgLatencyMs: 1200,
      totalExecutions: 24,
      successfulExecutions: 23,
      failedExecutions: 1,
      costIncurredINR: 4.5
    },
    memoryScope: 'CAMPAIGN',
    evaluationCriteria: ['Factual accuracy', 'Strategic relevance', 'Actionability', 'Compliance safety'],
    failurePolicy: 'RETRY',
    retryPolicy: { maxAttempts: 3, backoffBaseMs: 1000 },
    status: 'IDLE',
    version: '1.0.0'
  },
  {
    id: 'cnt-06',
    name: 'X Writer',
    category: 'CONTENT',
    role: 'Drafts punchy insights, industry commentary, and concise viral threads driving audience curiosity and clicks.',
    systemInstruction: `You are the ${'X Writer'}, operating within an autonomous AI Marketing Organization. Your core role is: ${'Drafts punchy insights, industry commentary, and concise viral threads driving audience curiosity and clicks.'}. Always deliver evidence-backed, actionable, and structured outputs tailored to the target market and business goals.`,
    capabilities: ['data_analysis', 'structured_synthesis', 'domain_reasoning'],
    allowedTools: ['database_read', 'database_write', 'evidence_retrieval', 'analytics_query'],
    inputSchema: { type: 'object', properties: { context: { type: 'string' }, goal: { type: 'object' } }, required: ['context'] },
    outputSchema: { type: 'object', properties: { findings: { type: 'array' }, recommendation: { type: 'string' }, confidence: { type: 'number' } }, required: ['recommendation', 'confidence'] },
    confidenceThreshold: 0.8,
    taskPermissions: ['READ_ONLY', 'PROPOSE_DECISION'],
    costBudget: 500,
    executionLimits: { maxRetries: 3, timeoutSeconds: 60, maxTokens: 4096 },
    performanceMetrics: {
      completionRate: 0.98,
      approvalRate: 0.95,
      avgLatencyMs: 1200,
      totalExecutions: 24,
      successfulExecutions: 23,
      failedExecutions: 1,
      costIncurredINR: 4.5
    },
    memoryScope: 'CAMPAIGN',
    evaluationCriteria: ['Factual accuracy', 'Strategic relevance', 'Actionability', 'Compliance safety'],
    failurePolicy: 'RETRY',
    retryPolicy: { maxAttempts: 3, backoffBaseMs: 1000 },
    status: 'IDLE',
    version: '1.0.0'
  },
  {
    id: 'cnt-07',
    name: 'Facebook Writer',
    category: 'CONTENT',
    role: 'Writes relatable community posts and local event promotions targeting family and neighbourhood interest groups.',
    systemInstruction: `You are the ${'Facebook Writer'}, operating within an autonomous AI Marketing Organization. Your core role is: ${'Writes relatable community posts and local event promotions targeting family and neighbourhood interest groups.'}. Always deliver evidence-backed, actionable, and structured outputs tailored to the target market and business goals.`,
    capabilities: ['data_analysis', 'structured_synthesis', 'domain_reasoning'],
    allowedTools: ['database_read', 'database_write', 'evidence_retrieval', 'analytics_query'],
    inputSchema: { type: 'object', properties: { context: { type: 'string' }, goal: { type: 'object' } }, required: ['context'] },
    outputSchema: { type: 'object', properties: { findings: { type: 'array' }, recommendation: { type: 'string' }, confidence: { type: 'number' } }, required: ['recommendation', 'confidence'] },
    confidenceThreshold: 0.8,
    taskPermissions: ['READ_ONLY', 'PROPOSE_DECISION'],
    costBudget: 500,
    executionLimits: { maxRetries: 3, timeoutSeconds: 60, maxTokens: 4096 },
    performanceMetrics: {
      completionRate: 0.98,
      approvalRate: 0.95,
      avgLatencyMs: 1200,
      totalExecutions: 24,
      successfulExecutions: 23,
      failedExecutions: 1,
      costIncurredINR: 4.5
    },
    memoryScope: 'CAMPAIGN',
    evaluationCriteria: ['Factual accuracy', 'Strategic relevance', 'Actionability', 'Compliance safety'],
    failurePolicy: 'RETRY',
    retryPolicy: { maxAttempts: 3, backoffBaseMs: 1000 },
    status: 'IDLE',
    version: '1.0.0'
  },
  {
    id: 'cnt-08',
    name: 'YouTube Script Writer',
    category: 'CONTENT',
    role: 'Develops structured video scripts with retention hooks, problem breakdowns, solutions, and visual cues.',
    systemInstruction: `You are the ${'YouTube Script Writer'}, operating within an autonomous AI Marketing Organization. Your core role is: ${'Develops structured video scripts with retention hooks, problem breakdowns, solutions, and visual cues.'}. Always deliver evidence-backed, actionable, and structured outputs tailored to the target market and business goals.`,
    capabilities: ['data_analysis', 'structured_synthesis', 'domain_reasoning'],
    allowedTools: ['database_read', 'database_write', 'evidence_retrieval', 'analytics_query'],
    inputSchema: { type: 'object', properties: { context: { type: 'string' }, goal: { type: 'object' } }, required: ['context'] },
    outputSchema: { type: 'object', properties: { findings: { type: 'array' }, recommendation: { type: 'string' }, confidence: { type: 'number' } }, required: ['recommendation', 'confidence'] },
    confidenceThreshold: 0.8,
    taskPermissions: ['READ_ONLY', 'PROPOSE_DECISION'],
    costBudget: 500,
    executionLimits: { maxRetries: 3, timeoutSeconds: 60, maxTokens: 4096 },
    performanceMetrics: {
      completionRate: 0.98,
      approvalRate: 0.95,
      avgLatencyMs: 1200,
      totalExecutions: 24,
      successfulExecutions: 23,
      failedExecutions: 1,
      costIncurredINR: 4.5
    },
    memoryScope: 'CAMPAIGN',
    evaluationCriteria: ['Factual accuracy', 'Strategic relevance', 'Actionability', 'Compliance safety'],
    failurePolicy: 'RETRY',
    retryPolicy: { maxAttempts: 3, backoffBaseMs: 1000 },
    status: 'IDLE',
    version: '1.0.0'
  },
  {
    id: 'cnt-09',
    name: 'Short Video Writer',
    category: 'CONTENT',
    role: 'Scripts high-energy 15-60s Reels, Shorts, and WhatsApp video hooks that stop scrolling within the first 3 seconds.',
    systemInstruction: `You are the ${'Short Video Writer'}, operating within an autonomous AI Marketing Organization. Your core role is: ${'Scripts high-energy 15-60s Reels, Shorts, and WhatsApp video hooks that stop scrolling within the first 3 seconds.'}. Always deliver evidence-backed, actionable, and structured outputs tailored to the target market and business goals.`,
    capabilities: ['data_analysis', 'structured_synthesis', 'domain_reasoning'],
    allowedTools: ['database_read', 'database_write', 'evidence_retrieval', 'analytics_query'],
    inputSchema: { type: 'object', properties: { context: { type: 'string' }, goal: { type: 'object' } }, required: ['context'] },
    outputSchema: { type: 'object', properties: { findings: { type: 'array' }, recommendation: { type: 'string' }, confidence: { type: 'number' } }, required: ['recommendation', 'confidence'] },
    confidenceThreshold: 0.8,
    taskPermissions: ['READ_ONLY', 'PROPOSE_DECISION'],
    costBudget: 500,
    executionLimits: { maxRetries: 3, timeoutSeconds: 60, maxTokens: 4096 },
    performanceMetrics: {
      completionRate: 0.98,
      approvalRate: 0.95,
      avgLatencyMs: 1200,
      totalExecutions: 24,
      successfulExecutions: 23,
      failedExecutions: 1,
      costIncurredINR: 4.5
    },
    memoryScope: 'CAMPAIGN',
    evaluationCriteria: ['Factual accuracy', 'Strategic relevance', 'Actionability', 'Compliance safety'],
    failurePolicy: 'RETRY',
    retryPolicy: { maxAttempts: 3, backoffBaseMs: 1000 },
    status: 'IDLE',
    version: '1.0.0'
  },
  {
    id: 'cnt-10',
    name: 'Email Writer',
    category: 'CONTENT',
    role: 'Composes lead nurturing email sequences, educational newsletters, and promotional broadcasts with high open rates.',
    systemInstruction: `You are the ${'Email Writer'}, operating within an autonomous AI Marketing Organization. Your core role is: ${'Composes lead nurturing email sequences, educational newsletters, and promotional broadcasts with high open rates.'}. Always deliver evidence-backed, actionable, and structured outputs tailored to the target market and business goals.`,
    capabilities: ['data_analysis', 'structured_synthesis', 'domain_reasoning'],
    allowedTools: ['database_read', 'database_write', 'evidence_retrieval', 'analytics_query'],
    inputSchema: { type: 'object', properties: { context: { type: 'string' }, goal: { type: 'object' } }, required: ['context'] },
    outputSchema: { type: 'object', properties: { findings: { type: 'array' }, recommendation: { type: 'string' }, confidence: { type: 'number' } }, required: ['recommendation', 'confidence'] },
    confidenceThreshold: 0.8,
    taskPermissions: ['READ_ONLY', 'PROPOSE_DECISION'],
    costBudget: 500,
    executionLimits: { maxRetries: 3, timeoutSeconds: 60, maxTokens: 4096 },
    performanceMetrics: {
      completionRate: 0.98,
      approvalRate: 0.95,
      avgLatencyMs: 1200,
      totalExecutions: 24,
      successfulExecutions: 23,
      failedExecutions: 1,
      costIncurredINR: 4.5
    },
    memoryScope: 'CAMPAIGN',
    evaluationCriteria: ['Factual accuracy', 'Strategic relevance', 'Actionability', 'Compliance safety'],
    failurePolicy: 'RETRY',
    retryPolicy: { maxAttempts: 3, backoffBaseMs: 1000 },
    status: 'IDLE',
    version: '1.0.0'
  },
  {
    id: 'cnt-11',
    name: 'WhatsApp Writer',
    category: 'CONTENT',
    role: 'Crafts conversational, personalized WhatsApp Business templates, broadcast alerts, and quick-reply flows.',
    systemInstruction: `You are the ${'WhatsApp Writer'}, operating within an autonomous AI Marketing Organization. Your core role is: ${'Crafts conversational, personalized WhatsApp Business templates, broadcast alerts, and quick-reply flows.'}. Always deliver evidence-backed, actionable, and structured outputs tailored to the target market and business goals.`,
    capabilities: ['data_analysis', 'structured_synthesis', 'domain_reasoning'],
    allowedTools: ['database_read', 'database_write', 'evidence_retrieval', 'analytics_query'],
    inputSchema: { type: 'object', properties: { context: { type: 'string' }, goal: { type: 'object' } }, required: ['context'] },
    outputSchema: { type: 'object', properties: { findings: { type: 'array' }, recommendation: { type: 'string' }, confidence: { type: 'number' } }, required: ['recommendation', 'confidence'] },
    confidenceThreshold: 0.8,
    taskPermissions: ['READ_ONLY', 'PROPOSE_DECISION'],
    costBudget: 500,
    executionLimits: { maxRetries: 3, timeoutSeconds: 60, maxTokens: 4096 },
    performanceMetrics: {
      completionRate: 0.98,
      approvalRate: 0.95,
      avgLatencyMs: 1200,
      totalExecutions: 24,
      successfulExecutions: 23,
      failedExecutions: 1,
      costIncurredINR: 4.5
    },
    memoryScope: 'CAMPAIGN',
    evaluationCriteria: ['Factual accuracy', 'Strategic relevance', 'Actionability', 'Compliance safety'],
    failurePolicy: 'RETRY',
    retryPolicy: { maxAttempts: 3, backoffBaseMs: 1000 },
    status: 'IDLE',
    version: '1.0.0'
  },
  {
    id: 'cnt-12',
    name: 'Ad Copy Writer',
    category: 'CONTENT',
    role: 'Writes high-CTR ad headlines, primary texts, and descriptions for Meta Ads, Google Ads, and local display campaigns.',
    systemInstruction: `You are the ${'Ad Copy Writer'}, operating within an autonomous AI Marketing Organization. Your core role is: ${'Writes high-CTR ad headlines, primary texts, and descriptions for Meta Ads, Google Ads, and local display campaigns.'}. Always deliver evidence-backed, actionable, and structured outputs tailored to the target market and business goals.`,
    capabilities: ['data_analysis', 'structured_synthesis', 'domain_reasoning'],
    allowedTools: ['database_read', 'database_write', 'evidence_retrieval', 'analytics_query'],
    inputSchema: { type: 'object', properties: { context: { type: 'string' }, goal: { type: 'object' } }, required: ['context'] },
    outputSchema: { type: 'object', properties: { findings: { type: 'array' }, recommendation: { type: 'string' }, confidence: { type: 'number' } }, required: ['recommendation', 'confidence'] },
    confidenceThreshold: 0.8,
    taskPermissions: ['READ_ONLY', 'PROPOSE_DECISION'],
    costBudget: 500,
    executionLimits: { maxRetries: 3, timeoutSeconds: 60, maxTokens: 4096 },
    performanceMetrics: {
      completionRate: 0.98,
      approvalRate: 0.95,
      avgLatencyMs: 1200,
      totalExecutions: 24,
      successfulExecutions: 23,
      failedExecutions: 1,
      costIncurredINR: 4.5
    },
    memoryScope: 'CAMPAIGN',
    evaluationCriteria: ['Factual accuracy', 'Strategic relevance', 'Actionability', 'Compliance safety'],
    failurePolicy: 'RETRY',
    retryPolicy: { maxAttempts: 3, backoffBaseMs: 1000 },
    status: 'IDLE',
    version: '1.0.0'
  },
  {
    id: 'cnt-13',
    name: 'Landing Page Writer',
    category: 'CONTENT',
    role: 'Builds high-converting direct-response sales pages with clear value props, social proof, and friction-free forms.',
    systemInstruction: `You are the ${'Landing Page Writer'}, operating within an autonomous AI Marketing Organization. Your core role is: ${'Builds high-converting direct-response sales pages with clear value props, social proof, and friction-free forms.'}. Always deliver evidence-backed, actionable, and structured outputs tailored to the target market and business goals.`,
    capabilities: ['data_analysis', 'structured_synthesis', 'domain_reasoning'],
    allowedTools: ['database_read', 'database_write', 'evidence_retrieval', 'analytics_query'],
    inputSchema: { type: 'object', properties: { context: { type: 'string' }, goal: { type: 'object' } }, required: ['context'] },
    outputSchema: { type: 'object', properties: { findings: { type: 'array' }, recommendation: { type: 'string' }, confidence: { type: 'number' } }, required: ['recommendation', 'confidence'] },
    confidenceThreshold: 0.8,
    taskPermissions: ['READ_ONLY', 'PROPOSE_DECISION'],
    costBudget: 500,
    executionLimits: { maxRetries: 3, timeoutSeconds: 60, maxTokens: 4096 },
    performanceMetrics: {
      completionRate: 0.98,
      approvalRate: 0.95,
      avgLatencyMs: 1200,
      totalExecutions: 24,
      successfulExecutions: 23,
      failedExecutions: 1,
      costIncurredINR: 4.5
    },
    memoryScope: 'CAMPAIGN',
    evaluationCriteria: ['Factual accuracy', 'Strategic relevance', 'Actionability', 'Compliance safety'],
    failurePolicy: 'RETRY',
    retryPolicy: { maxAttempts: 3, backoffBaseMs: 1000 },
    status: 'IDLE',
    version: '1.0.0'
  },
  {
    id: 'cnt-14',
    name: 'Case Study Writer',
    category: 'CONTENT',
    role: 'Transforms patient and customer success transformations into compelling before/after transformation stories.',
    systemInstruction: `You are the ${'Case Study Writer'}, operating within an autonomous AI Marketing Organization. Your core role is: ${'Transforms patient and customer success transformations into compelling before/after transformation stories.'}. Always deliver evidence-backed, actionable, and structured outputs tailored to the target market and business goals.`,
    capabilities: ['data_analysis', 'structured_synthesis', 'domain_reasoning'],
    allowedTools: ['database_read', 'database_write', 'evidence_retrieval', 'analytics_query'],
    inputSchema: { type: 'object', properties: { context: { type: 'string' }, goal: { type: 'object' } }, required: ['context'] },
    outputSchema: { type: 'object', properties: { findings: { type: 'array' }, recommendation: { type: 'string' }, confidence: { type: 'number' } }, required: ['recommendation', 'confidence'] },
    confidenceThreshold: 0.8,
    taskPermissions: ['READ_ONLY', 'PROPOSE_DECISION'],
    costBudget: 500,
    executionLimits: { maxRetries: 3, timeoutSeconds: 60, maxTokens: 4096 },
    performanceMetrics: {
      completionRate: 0.98,
      approvalRate: 0.95,
      avgLatencyMs: 1200,
      totalExecutions: 24,
      successfulExecutions: 23,
      failedExecutions: 1,
      costIncurredINR: 4.5
    },
    memoryScope: 'CAMPAIGN',
    evaluationCriteria: ['Factual accuracy', 'Strategic relevance', 'Actionability', 'Compliance safety'],
    failurePolicy: 'RETRY',
    retryPolicy: { maxAttempts: 3, backoffBaseMs: 1000 },
    status: 'IDLE',
    version: '1.0.0'
  },
  {
    id: 'cnt-15',
    name: 'Thought Leadership Writer',
    category: 'CONTENT',
    role: 'Articulates proprietary methodologies, expert clinic opinions, and future-looking sector perspectives.',
    systemInstruction: `You are the ${'Thought Leadership Writer'}, operating within an autonomous AI Marketing Organization. Your core role is: ${'Articulates proprietary methodologies, expert clinic opinions, and future-looking sector perspectives.'}. Always deliver evidence-backed, actionable, and structured outputs tailored to the target market and business goals.`,
    capabilities: ['data_analysis', 'structured_synthesis', 'domain_reasoning'],
    allowedTools: ['database_read', 'database_write', 'evidence_retrieval', 'analytics_query'],
    inputSchema: { type: 'object', properties: { context: { type: 'string' }, goal: { type: 'object' } }, required: ['context'] },
    outputSchema: { type: 'object', properties: { findings: { type: 'array' }, recommendation: { type: 'string' }, confidence: { type: 'number' } }, required: ['recommendation', 'confidence'] },
    confidenceThreshold: 0.8,
    taskPermissions: ['READ_ONLY', 'PROPOSE_DECISION'],
    costBudget: 500,
    executionLimits: { maxRetries: 3, timeoutSeconds: 60, maxTokens: 4096 },
    performanceMetrics: {
      completionRate: 0.98,
      approvalRate: 0.95,
      avgLatencyMs: 1200,
      totalExecutions: 24,
      successfulExecutions: 23,
      failedExecutions: 1,
      costIncurredINR: 4.5
    },
    memoryScope: 'CAMPAIGN',
    evaluationCriteria: ['Factual accuracy', 'Strategic relevance', 'Actionability', 'Compliance safety'],
    failurePolicy: 'RETRY',
    retryPolicy: { maxAttempts: 3, backoffBaseMs: 1000 },
    status: 'IDLE',
    version: '1.0.0'
  },
  {
    id: 'cnt-16',
    name: 'Hook Generator',
    category: 'CONTENT',
    role: 'Generates dozens of psychologically tested headline hooks, opening lines, and pattern interrupts for any topic.',
    systemInstruction: `You are the ${'Hook Generator'}, operating within an autonomous AI Marketing Organization. Your core role is: ${'Generates dozens of psychologically tested headline hooks, opening lines, and pattern interrupts for any topic.'}. Always deliver evidence-backed, actionable, and structured outputs tailored to the target market and business goals.`,
    capabilities: ['data_analysis', 'structured_synthesis', 'domain_reasoning'],
    allowedTools: ['database_read', 'database_write', 'evidence_retrieval', 'analytics_query'],
    inputSchema: { type: 'object', properties: { context: { type: 'string' }, goal: { type: 'object' } }, required: ['context'] },
    outputSchema: { type: 'object', properties: { findings: { type: 'array' }, recommendation: { type: 'string' }, confidence: { type: 'number' } }, required: ['recommendation', 'confidence'] },
    confidenceThreshold: 0.8,
    taskPermissions: ['READ_ONLY', 'PROPOSE_DECISION'],
    costBudget: 500,
    executionLimits: { maxRetries: 3, timeoutSeconds: 60, maxTokens: 4096 },
    performanceMetrics: {
      completionRate: 0.98,
      approvalRate: 0.95,
      avgLatencyMs: 1200,
      totalExecutions: 24,
      successfulExecutions: 23,
      failedExecutions: 1,
      costIncurredINR: 4.5
    },
    memoryScope: 'CAMPAIGN',
    evaluationCriteria: ['Factual accuracy', 'Strategic relevance', 'Actionability', 'Compliance safety'],
    failurePolicy: 'RETRY',
    retryPolicy: { maxAttempts: 3, backoffBaseMs: 1000 },
    status: 'IDLE',
    version: '1.0.0'
  },
  {
    id: 'cnt-17',
    name: 'CTA Specialist',
    category: 'CONTENT',
    role: 'Engineers high-urgency, low-friction calls-to-action tailored to WhatsApp booking, call scheduling, or lead forms.',
    systemInstruction: `You are the ${'CTA Specialist'}, operating within an autonomous AI Marketing Organization. Your core role is: ${'Engineers high-urgency, low-friction calls-to-action tailored to WhatsApp booking, call scheduling, or lead forms.'}. Always deliver evidence-backed, actionable, and structured outputs tailored to the target market and business goals.`,
    capabilities: ['data_analysis', 'structured_synthesis', 'domain_reasoning'],
    allowedTools: ['database_read', 'database_write', 'evidence_retrieval', 'analytics_query'],
    inputSchema: { type: 'object', properties: { context: { type: 'string' }, goal: { type: 'object' } }, required: ['context'] },
    outputSchema: { type: 'object', properties: { findings: { type: 'array' }, recommendation: { type: 'string' }, confidence: { type: 'number' } }, required: ['recommendation', 'confidence'] },
    confidenceThreshold: 0.8,
    taskPermissions: ['READ_ONLY', 'PROPOSE_DECISION'],
    costBudget: 500,
    executionLimits: { maxRetries: 3, timeoutSeconds: 60, maxTokens: 4096 },
    performanceMetrics: {
      completionRate: 0.98,
      approvalRate: 0.95,
      avgLatencyMs: 1200,
      totalExecutions: 24,
      successfulExecutions: 23,
      failedExecutions: 1,
      costIncurredINR: 4.5
    },
    memoryScope: 'CAMPAIGN',
    evaluationCriteria: ['Factual accuracy', 'Strategic relevance', 'Actionability', 'Compliance safety'],
    failurePolicy: 'RETRY',
    retryPolicy: { maxAttempts: 3, backoffBaseMs: 1000 },
    status: 'IDLE',
    version: '1.0.0'
  },
  {
    id: 'cnt-18',
    name: 'Repurposing Agent',
    category: 'CONTENT',
    role: 'Transforms one pillar asset (e.g. video or case study) into 10+ modular formats across social, email, and chat.',
    systemInstruction: `You are the ${'Repurposing Agent'}, operating within an autonomous AI Marketing Organization. Your core role is: ${'Transforms one pillar asset (e.g. video or case study) into 10+ modular formats across social, email, and chat.'}. Always deliver evidence-backed, actionable, and structured outputs tailored to the target market and business goals.`,
    capabilities: ['data_analysis', 'structured_synthesis', 'domain_reasoning'],
    allowedTools: ['database_read', 'database_write', 'evidence_retrieval', 'analytics_query'],
    inputSchema: { type: 'object', properties: { context: { type: 'string' }, goal: { type: 'object' } }, required: ['context'] },
    outputSchema: { type: 'object', properties: { findings: { type: 'array' }, recommendation: { type: 'string' }, confidence: { type: 'number' } }, required: ['recommendation', 'confidence'] },
    confidenceThreshold: 0.8,
    taskPermissions: ['READ_ONLY', 'PROPOSE_DECISION'],
    costBudget: 500,
    executionLimits: { maxRetries: 3, timeoutSeconds: 60, maxTokens: 4096 },
    performanceMetrics: {
      completionRate: 0.98,
      approvalRate: 0.95,
      avgLatencyMs: 1200,
      totalExecutions: 24,
      successfulExecutions: 23,
      failedExecutions: 1,
      costIncurredINR: 4.5
    },
    memoryScope: 'CAMPAIGN',
    evaluationCriteria: ['Factual accuracy', 'Strategic relevance', 'Actionability', 'Compliance safety'],
    failurePolicy: 'RETRY',
    retryPolicy: { maxAttempts: 3, backoffBaseMs: 1000 },
    status: 'IDLE',
    version: '1.0.0'
  },
  {
    id: 'cnt-19',
    name: 'Brand Voice Agent',
    category: 'CONTENT',
    role: 'Audits every drafted asset against established brand tone, vocabulary rules, empathy level, and professionalism.',
    systemInstruction: `You are the ${'Brand Voice Agent'}, operating within an autonomous AI Marketing Organization. Your core role is: ${'Audits every drafted asset against established brand tone, vocabulary rules, empathy level, and professionalism.'}. Always deliver evidence-backed, actionable, and structured outputs tailored to the target market and business goals.`,
    capabilities: ['data_analysis', 'structured_synthesis', 'domain_reasoning'],
    allowedTools: ['database_read', 'database_write', 'evidence_retrieval', 'analytics_query'],
    inputSchema: { type: 'object', properties: { context: { type: 'string' }, goal: { type: 'object' } }, required: ['context'] },
    outputSchema: { type: 'object', properties: { findings: { type: 'array' }, recommendation: { type: 'string' }, confidence: { type: 'number' } }, required: ['recommendation', 'confidence'] },
    confidenceThreshold: 0.8,
    taskPermissions: ['READ_ONLY', 'PROPOSE_DECISION'],
    costBudget: 500,
    executionLimits: { maxRetries: 3, timeoutSeconds: 60, maxTokens: 4096 },
    performanceMetrics: {
      completionRate: 0.98,
      approvalRate: 0.95,
      avgLatencyMs: 1200,
      totalExecutions: 24,
      successfulExecutions: 23,
      failedExecutions: 1,
      costIncurredINR: 4.5
    },
    memoryScope: 'CAMPAIGN',
    evaluationCriteria: ['Factual accuracy', 'Strategic relevance', 'Actionability', 'Compliance safety'],
    failurePolicy: 'RETRY',
    retryPolicy: { maxAttempts: 3, backoffBaseMs: 1000 },
    status: 'IDLE',
    version: '1.0.0'
  },
  {
    id: 'cnt-20',
    name: 'Content QA Agent',
    category: 'CONTENT',
    role: 'Validates factual accuracy, grammar, compliance, claims verification, and link/CTA functionality before approval.',
    systemInstruction: `You are the ${'Content QA Agent'}, operating within an autonomous AI Marketing Organization. Your core role is: ${'Validates factual accuracy, grammar, compliance, claims verification, and link/CTA functionality before approval.'}. Always deliver evidence-backed, actionable, and structured outputs tailored to the target market and business goals.`,
    capabilities: ['data_analysis', 'structured_synthesis', 'domain_reasoning'],
    allowedTools: ['database_read', 'database_write', 'evidence_retrieval', 'analytics_query'],
    inputSchema: { type: 'object', properties: { context: { type: 'string' }, goal: { type: 'object' } }, required: ['context'] },
    outputSchema: { type: 'object', properties: { findings: { type: 'array' }, recommendation: { type: 'string' }, confidence: { type: 'number' } }, required: ['recommendation', 'confidence'] },
    confidenceThreshold: 0.8,
    taskPermissions: ['READ_ONLY', 'PROPOSE_DECISION'],
    costBudget: 500,
    executionLimits: { maxRetries: 3, timeoutSeconds: 60, maxTokens: 4096 },
    performanceMetrics: {
      completionRate: 0.98,
      approvalRate: 0.95,
      avgLatencyMs: 1200,
      totalExecutions: 24,
      successfulExecutions: 23,
      failedExecutions: 1,
      costIncurredINR: 4.5
    },
    memoryScope: 'CAMPAIGN',
    evaluationCriteria: ['Factual accuracy', 'Strategic relevance', 'Actionability', 'Compliance safety'],
    failurePolicy: 'RETRY',
    retryPolicy: { maxAttempts: 3, backoffBaseMs: 1000 },
    status: 'IDLE',
    version: '1.0.0'
  }
  ,
  // Marketing & Growth (20)
  {
    id: 'mkt-01',
    name: 'Marketing Strategist',
    category: 'MARKETING_GROWTH',
    role: 'Architects overarching marketing roadmaps connecting business goals to positioning, channels, and budgets.',
    systemInstruction: `You are the ${'Marketing Strategist'}, operating within an autonomous AI Marketing Organization. Your core role is: ${'Architects overarching marketing roadmaps connecting business goals to positioning, channels, and budgets.'}. Always deliver evidence-backed, actionable, and structured outputs tailored to the target market and business goals.`,
    capabilities: ['data_analysis', 'structured_synthesis', 'domain_reasoning'],
    allowedTools: ['database_read', 'database_write', 'evidence_retrieval', 'analytics_query'],
    inputSchema: { type: 'object', properties: { context: { type: 'string' }, goal: { type: 'object' } }, required: ['context'] },
    outputSchema: { type: 'object', properties: { findings: { type: 'array' }, recommendation: { type: 'string' }, confidence: { type: 'number' } }, required: ['recommendation', 'confidence'] },
    confidenceThreshold: 0.8,
    taskPermissions: ['READ_ONLY', 'PROPOSE_DECISION'],
    costBudget: 500,
    executionLimits: { maxRetries: 3, timeoutSeconds: 60, maxTokens: 4096 },
    performanceMetrics: {
      completionRate: 0.98,
      approvalRate: 0.95,
      avgLatencyMs: 1200,
      totalExecutions: 24,
      successfulExecutions: 23,
      failedExecutions: 1,
      costIncurredINR: 4.5
    },
    memoryScope: 'BUSINESS',
    evaluationCriteria: ['Factual accuracy', 'Strategic relevance', 'Actionability', 'Compliance safety'],
    failurePolicy: 'RETRY',
    retryPolicy: { maxAttempts: 3, backoffBaseMs: 1000 },
    status: 'IDLE',
    version: '1.0.0'
  },
  {
    id: 'mkt-02',
    name: 'Campaign Strategist',
    category: 'MARKETING_GROWTH',
    role: 'Designs integrated marketing campaigns with defined flight dates, channel mixes, offers, and conversion KPIs.',
    systemInstruction: `You are the ${'Campaign Strategist'}, operating within an autonomous AI Marketing Organization. Your core role is: ${'Designs integrated marketing campaigns with defined flight dates, channel mixes, offers, and conversion KPIs.'}. Always deliver evidence-backed, actionable, and structured outputs tailored to the target market and business goals.`,
    capabilities: ['data_analysis', 'structured_synthesis', 'domain_reasoning'],
    allowedTools: ['database_read', 'database_write', 'evidence_retrieval', 'analytics_query'],
    inputSchema: { type: 'object', properties: { context: { type: 'string' }, goal: { type: 'object' } }, required: ['context'] },
    outputSchema: { type: 'object', properties: { findings: { type: 'array' }, recommendation: { type: 'string' }, confidence: { type: 'number' } }, required: ['recommendation', 'confidence'] },
    confidenceThreshold: 0.8,
    taskPermissions: ['READ_ONLY', 'PROPOSE_DECISION'],
    costBudget: 500,
    executionLimits: { maxRetries: 3, timeoutSeconds: 60, maxTokens: 4096 },
    performanceMetrics: {
      completionRate: 0.98,
      approvalRate: 0.95,
      avgLatencyMs: 1200,
      totalExecutions: 24,
      successfulExecutions: 23,
      failedExecutions: 1,
      costIncurredINR: 4.5
    },
    memoryScope: 'BUSINESS',
    evaluationCriteria: ['Factual accuracy', 'Strategic relevance', 'Actionability', 'Compliance safety'],
    failurePolicy: 'RETRY',
    retryPolicy: { maxAttempts: 3, backoffBaseMs: 1000 },
    status: 'IDLE',
    version: '1.0.0'
  },
  {
    id: 'mkt-03',
    name: 'Growth Strategist',
    category: 'MARKETING_GROWTH',
    role: 'Identifies compounding growth flywheels, referral incentives, and rapid customer acquisition tactics.',
    systemInstruction: `You are the ${'Growth Strategist'}, operating within an autonomous AI Marketing Organization. Your core role is: ${'Identifies compounding growth flywheels, referral incentives, and rapid customer acquisition tactics.'}. Always deliver evidence-backed, actionable, and structured outputs tailored to the target market and business goals.`,
    capabilities: ['data_analysis', 'structured_synthesis', 'domain_reasoning'],
    allowedTools: ['database_read', 'database_write', 'evidence_retrieval', 'analytics_query'],
    inputSchema: { type: 'object', properties: { context: { type: 'string' }, goal: { type: 'object' } }, required: ['context'] },
    outputSchema: { type: 'object', properties: { findings: { type: 'array' }, recommendation: { type: 'string' }, confidence: { type: 'number' } }, required: ['recommendation', 'confidence'] },
    confidenceThreshold: 0.8,
    taskPermissions: ['READ_ONLY', 'PROPOSE_DECISION'],
    costBudget: 500,
    executionLimits: { maxRetries: 3, timeoutSeconds: 60, maxTokens: 4096 },
    performanceMetrics: {
      completionRate: 0.98,
      approvalRate: 0.95,
      avgLatencyMs: 1200,
      totalExecutions: 24,
      successfulExecutions: 23,
      failedExecutions: 1,
      costIncurredINR: 4.5
    },
    memoryScope: 'BUSINESS',
    evaluationCriteria: ['Factual accuracy', 'Strategic relevance', 'Actionability', 'Compliance safety'],
    failurePolicy: 'RETRY',
    retryPolicy: { maxAttempts: 3, backoffBaseMs: 1000 },
    status: 'IDLE',
    version: '1.0.0'
  },
  {
    id: 'mkt-04',
    name: 'SEO Strategist',
    category: 'MARKETING_GROWTH',
    role: 'Plans technical SEO, local Google Business Profile rank improvements, and backlink authority building.',
    systemInstruction: `You are the ${'SEO Strategist'}, operating within an autonomous AI Marketing Organization. Your core role is: ${'Plans technical SEO, local Google Business Profile rank improvements, and backlink authority building.'}. Always deliver evidence-backed, actionable, and structured outputs tailored to the target market and business goals.`,
    capabilities: ['data_analysis', 'structured_synthesis', 'domain_reasoning'],
    allowedTools: ['database_read', 'database_write', 'evidence_retrieval', 'analytics_query'],
    inputSchema: { type: 'object', properties: { context: { type: 'string' }, goal: { type: 'object' } }, required: ['context'] },
    outputSchema: { type: 'object', properties: { findings: { type: 'array' }, recommendation: { type: 'string' }, confidence: { type: 'number' } }, required: ['recommendation', 'confidence'] },
    confidenceThreshold: 0.8,
    taskPermissions: ['READ_ONLY', 'PROPOSE_DECISION'],
    costBudget: 500,
    executionLimits: { maxRetries: 3, timeoutSeconds: 60, maxTokens: 4096 },
    performanceMetrics: {
      completionRate: 0.98,
      approvalRate: 0.95,
      avgLatencyMs: 1200,
      totalExecutions: 24,
      successfulExecutions: 23,
      failedExecutions: 1,
      costIncurredINR: 4.5
    },
    memoryScope: 'BUSINESS',
    evaluationCriteria: ['Factual accuracy', 'Strategic relevance', 'Actionability', 'Compliance safety'],
    failurePolicy: 'RETRY',
    retryPolicy: { maxAttempts: 3, backoffBaseMs: 1000 },
    status: 'IDLE',
    version: '1.0.0'
  },
  {
    id: 'mkt-05',
    name: 'Social Strategist',
    category: 'MARKETING_GROWTH',
    role: 'Directs organic social media distribution, publishing cadence, community interactions, and brand resonance.',
    systemInstruction: `You are the ${'Social Strategist'}, operating within an autonomous AI Marketing Organization. Your core role is: ${'Directs organic social media distribution, publishing cadence, community interactions, and brand resonance.'}. Always deliver evidence-backed, actionable, and structured outputs tailored to the target market and business goals.`,
    capabilities: ['data_analysis', 'structured_synthesis', 'domain_reasoning'],
    allowedTools: ['database_read', 'database_write', 'evidence_retrieval', 'analytics_query'],
    inputSchema: { type: 'object', properties: { context: { type: 'string' }, goal: { type: 'object' } }, required: ['context'] },
    outputSchema: { type: 'object', properties: { findings: { type: 'array' }, recommendation: { type: 'string' }, confidence: { type: 'number' } }, required: ['recommendation', 'confidence'] },
    confidenceThreshold: 0.8,
    taskPermissions: ['READ_ONLY', 'PROPOSE_DECISION'],
    costBudget: 500,
    executionLimits: { maxRetries: 3, timeoutSeconds: 60, maxTokens: 4096 },
    performanceMetrics: {
      completionRate: 0.98,
      approvalRate: 0.95,
      avgLatencyMs: 1200,
      totalExecutions: 24,
      successfulExecutions: 23,
      failedExecutions: 1,
      costIncurredINR: 4.5
    },
    memoryScope: 'BUSINESS',
    evaluationCriteria: ['Factual accuracy', 'Strategic relevance', 'Actionability', 'Compliance safety'],
    failurePolicy: 'RETRY',
    retryPolicy: { maxAttempts: 3, backoffBaseMs: 1000 },
    status: 'IDLE',
    version: '1.0.0'
  },
  {
    id: 'mkt-06',
    name: 'Paid Ads Strategist',
    category: 'MARKETING_GROWTH',
    role: 'Structures paid media campaigns across Meta Ads and Google Ads with target CPA/ROAS and budget capping in INR.',
    systemInstruction: `You are the ${'Paid Ads Strategist'}, operating within an autonomous AI Marketing Organization. Your core role is: ${'Structures paid media campaigns across Meta Ads and Google Ads with target CPA/ROAS and budget capping in INR.'}. Always deliver evidence-backed, actionable, and structured outputs tailored to the target market and business goals.`,
    capabilities: ['data_analysis', 'structured_synthesis', 'domain_reasoning'],
    allowedTools: ['database_read', 'database_write', 'evidence_retrieval', 'analytics_query'],
    inputSchema: { type: 'object', properties: { context: { type: 'string' }, goal: { type: 'object' } }, required: ['context'] },
    outputSchema: { type: 'object', properties: { findings: { type: 'array' }, recommendation: { type: 'string' }, confidence: { type: 'number' } }, required: ['recommendation', 'confidence'] },
    confidenceThreshold: 0.8,
    taskPermissions: ['READ_ONLY', 'PROPOSE_DECISION'],
    costBudget: 500,
    executionLimits: { maxRetries: 3, timeoutSeconds: 60, maxTokens: 4096 },
    performanceMetrics: {
      completionRate: 0.98,
      approvalRate: 0.95,
      avgLatencyMs: 1200,
      totalExecutions: 24,
      successfulExecutions: 23,
      failedExecutions: 1,
      costIncurredINR: 4.5
    },
    memoryScope: 'BUSINESS',
    evaluationCriteria: ['Factual accuracy', 'Strategic relevance', 'Actionability', 'Compliance safety'],
    failurePolicy: 'RETRY',
    retryPolicy: { maxAttempts: 3, backoffBaseMs: 1000 },
    status: 'IDLE',
    version: '1.0.0'
  },
  {
    id: 'mkt-07',
    name: 'Email Strategist',
    category: 'MARKETING_GROWTH',
    role: 'Builds automated email lifecycle drips, churn prevention flows, and re-engagement campaigns.',
    systemInstruction: `You are the ${'Email Strategist'}, operating within an autonomous AI Marketing Organization. Your core role is: ${'Builds automated email lifecycle drips, churn prevention flows, and re-engagement campaigns.'}. Always deliver evidence-backed, actionable, and structured outputs tailored to the target market and business goals.`,
    capabilities: ['data_analysis', 'structured_synthesis', 'domain_reasoning'],
    allowedTools: ['database_read', 'database_write', 'evidence_retrieval', 'analytics_query'],
    inputSchema: { type: 'object', properties: { context: { type: 'string' }, goal: { type: 'object' } }, required: ['context'] },
    outputSchema: { type: 'object', properties: { findings: { type: 'array' }, recommendation: { type: 'string' }, confidence: { type: 'number' } }, required: ['recommendation', 'confidence'] },
    confidenceThreshold: 0.8,
    taskPermissions: ['READ_ONLY', 'PROPOSE_DECISION'],
    costBudget: 500,
    executionLimits: { maxRetries: 3, timeoutSeconds: 60, maxTokens: 4096 },
    performanceMetrics: {
      completionRate: 0.98,
      approvalRate: 0.95,
      avgLatencyMs: 1200,
      totalExecutions: 24,
      successfulExecutions: 23,
      failedExecutions: 1,
      costIncurredINR: 4.5
    },
    memoryScope: 'BUSINESS',
    evaluationCriteria: ['Factual accuracy', 'Strategic relevance', 'Actionability', 'Compliance safety'],
    failurePolicy: 'RETRY',
    retryPolicy: { maxAttempts: 3, backoffBaseMs: 1000 },
    status: 'IDLE',
    version: '1.0.0'
  },
  {
    id: 'mkt-08',
    name: 'WhatsApp Strategist',
    category: 'MARKETING_GROWTH',
    role: 'Architects conversational WhatsApp funnels, automated greeting flows, appointment reminders, and follow-up drips.',
    systemInstruction: `You are the ${'WhatsApp Strategist'}, operating within an autonomous AI Marketing Organization. Your core role is: ${'Architects conversational WhatsApp funnels, automated greeting flows, appointment reminders, and follow-up drips.'}. Always deliver evidence-backed, actionable, and structured outputs tailored to the target market and business goals.`,
    capabilities: ['data_analysis', 'structured_synthesis', 'domain_reasoning'],
    allowedTools: ['database_read', 'database_write', 'evidence_retrieval', 'analytics_query'],
    inputSchema: { type: 'object', properties: { context: { type: 'string' }, goal: { type: 'object' } }, required: ['context'] },
    outputSchema: { type: 'object', properties: { findings: { type: 'array' }, recommendation: { type: 'string' }, confidence: { type: 'number' } }, required: ['recommendation', 'confidence'] },
    confidenceThreshold: 0.8,
    taskPermissions: ['READ_ONLY', 'PROPOSE_DECISION'],
    costBudget: 500,
    executionLimits: { maxRetries: 3, timeoutSeconds: 60, maxTokens: 4096 },
    performanceMetrics: {
      completionRate: 0.98,
      approvalRate: 0.95,
      avgLatencyMs: 1200,
      totalExecutions: 24,
      successfulExecutions: 23,
      failedExecutions: 1,
      costIncurredINR: 4.5
    },
    memoryScope: 'BUSINESS',
    evaluationCriteria: ['Factual accuracy', 'Strategic relevance', 'Actionability', 'Compliance safety'],
    failurePolicy: 'RETRY',
    retryPolicy: { maxAttempts: 3, backoffBaseMs: 1000 },
    status: 'IDLE',
    version: '1.0.0'
  },
  {
    id: 'mkt-09',
    name: 'Funnel Strategist',
    category: 'MARKETING_GROWTH',
    role: 'Maps full-funnel customer journeys from first awareness touchpoint to qualified lead, appointment, and repeat purchase.',
    systemInstruction: `You are the ${'Funnel Strategist'}, operating within an autonomous AI Marketing Organization. Your core role is: ${'Maps full-funnel customer journeys from first awareness touchpoint to qualified lead, appointment, and repeat purchase.'}. Always deliver evidence-backed, actionable, and structured outputs tailored to the target market and business goals.`,
    capabilities: ['data_analysis', 'structured_synthesis', 'domain_reasoning'],
    allowedTools: ['database_read', 'database_write', 'evidence_retrieval', 'analytics_query'],
    inputSchema: { type: 'object', properties: { context: { type: 'string' }, goal: { type: 'object' } }, required: ['context'] },
    outputSchema: { type: 'object', properties: { findings: { type: 'array' }, recommendation: { type: 'string' }, confidence: { type: 'number' } }, required: ['recommendation', 'confidence'] },
    confidenceThreshold: 0.8,
    taskPermissions: ['READ_ONLY', 'PROPOSE_DECISION'],
    costBudget: 500,
    executionLimits: { maxRetries: 3, timeoutSeconds: 60, maxTokens: 4096 },
    performanceMetrics: {
      completionRate: 0.98,
      approvalRate: 0.95,
      avgLatencyMs: 1200,
      totalExecutions: 24,
      successfulExecutions: 23,
      failedExecutions: 1,
      costIncurredINR: 4.5
    },
    memoryScope: 'BUSINESS',
    evaluationCriteria: ['Factual accuracy', 'Strategic relevance', 'Actionability', 'Compliance safety'],
    failurePolicy: 'RETRY',
    retryPolicy: { maxAttempts: 3, backoffBaseMs: 1000 },
    status: 'IDLE',
    version: '1.0.0'
  },
  {
    id: 'mkt-10',
    name: 'Conversion Strategist',
    category: 'MARKETING_GROWTH',
    role: 'Optimizes conversion rate (CRO) on landing pages, WhatsApp opt-ins, and consultation booking forms.',
    systemInstruction: `You are the ${'Conversion Strategist'}, operating within an autonomous AI Marketing Organization. Your core role is: ${'Optimizes conversion rate (CRO) on landing pages, WhatsApp opt-ins, and consultation booking forms.'}. Always deliver evidence-backed, actionable, and structured outputs tailored to the target market and business goals.`,
    capabilities: ['data_analysis', 'structured_synthesis', 'domain_reasoning'],
    allowedTools: ['database_read', 'database_write', 'evidence_retrieval', 'analytics_query'],
    inputSchema: { type: 'object', properties: { context: { type: 'string' }, goal: { type: 'object' } }, required: ['context'] },
    outputSchema: { type: 'object', properties: { findings: { type: 'array' }, recommendation: { type: 'string' }, confidence: { type: 'number' } }, required: ['recommendation', 'confidence'] },
    confidenceThreshold: 0.8,
    taskPermissions: ['READ_ONLY', 'PROPOSE_DECISION'],
    costBudget: 500,
    executionLimits: { maxRetries: 3, timeoutSeconds: 60, maxTokens: 4096 },
    performanceMetrics: {
      completionRate: 0.98,
      approvalRate: 0.95,
      avgLatencyMs: 1200,
      totalExecutions: 24,
      successfulExecutions: 23,
      failedExecutions: 1,
      costIncurredINR: 4.5
    },
    memoryScope: 'BUSINESS',
    evaluationCriteria: ['Factual accuracy', 'Strategic relevance', 'Actionability', 'Compliance safety'],
    failurePolicy: 'RETRY',
    retryPolicy: { maxAttempts: 3, backoffBaseMs: 1000 },
    status: 'IDLE',
    version: '1.0.0'
  },
  {
    id: 'mkt-11',
    name: 'Offer Strategist',
    category: 'MARKETING_GROWTH',
    role: 'Engineers irresistible core offers, risk reversals, free value add-ons, and payment plans tailored to Indian buyers.',
    systemInstruction: `You are the ${'Offer Strategist'}, operating within an autonomous AI Marketing Organization. Your core role is: ${'Engineers irresistible core offers, risk reversals, free value add-ons, and payment plans tailored to Indian buyers.'}. Always deliver evidence-backed, actionable, and structured outputs tailored to the target market and business goals.`,
    capabilities: ['data_analysis', 'structured_synthesis', 'domain_reasoning'],
    allowedTools: ['database_read', 'database_write', 'evidence_retrieval', 'analytics_query'],
    inputSchema: { type: 'object', properties: { context: { type: 'string' }, goal: { type: 'object' } }, required: ['context'] },
    outputSchema: { type: 'object', properties: { findings: { type: 'array' }, recommendation: { type: 'string' }, confidence: { type: 'number' } }, required: ['recommendation', 'confidence'] },
    confidenceThreshold: 0.8,
    taskPermissions: ['READ_ONLY', 'PROPOSE_DECISION'],
    costBudget: 500,
    executionLimits: { maxRetries: 3, timeoutSeconds: 60, maxTokens: 4096 },
    performanceMetrics: {
      completionRate: 0.98,
      approvalRate: 0.95,
      avgLatencyMs: 1200,
      totalExecutions: 24,
      successfulExecutions: 23,
      failedExecutions: 1,
      costIncurredINR: 4.5
    },
    memoryScope: 'BUSINESS',
    evaluationCriteria: ['Factual accuracy', 'Strategic relevance', 'Actionability', 'Compliance safety'],
    failurePolicy: 'RETRY',
    retryPolicy: { maxAttempts: 3, backoffBaseMs: 1000 },
    status: 'IDLE',
    version: '1.0.0'
  },
  {
    id: 'mkt-12',
    name: 'Positioning Strategist',
    category: 'MARKETING_GROWTH',
    role: 'Defines distinct market positioning that differentiates the business from local commodity competitors.',
    systemInstruction: `You are the ${'Positioning Strategist'}, operating within an autonomous AI Marketing Organization. Your core role is: ${'Defines distinct market positioning that differentiates the business from local commodity competitors.'}. Always deliver evidence-backed, actionable, and structured outputs tailored to the target market and business goals.`,
    capabilities: ['data_analysis', 'structured_synthesis', 'domain_reasoning'],
    allowedTools: ['database_read', 'database_write', 'evidence_retrieval', 'analytics_query'],
    inputSchema: { type: 'object', properties: { context: { type: 'string' }, goal: { type: 'object' } }, required: ['context'] },
    outputSchema: { type: 'object', properties: { findings: { type: 'array' }, recommendation: { type: 'string' }, confidence: { type: 'number' } }, required: ['recommendation', 'confidence'] },
    confidenceThreshold: 0.8,
    taskPermissions: ['READ_ONLY', 'PROPOSE_DECISION'],
    costBudget: 500,
    executionLimits: { maxRetries: 3, timeoutSeconds: 60, maxTokens: 4096 },
    performanceMetrics: {
      completionRate: 0.98,
      approvalRate: 0.95,
      avgLatencyMs: 1200,
      totalExecutions: 24,
      successfulExecutions: 23,
      failedExecutions: 1,
      costIncurredINR: 4.5
    },
    memoryScope: 'BUSINESS',
    evaluationCriteria: ['Factual accuracy', 'Strategic relevance', 'Actionability', 'Compliance safety'],
    failurePolicy: 'RETRY',
    retryPolicy: { maxAttempts: 3, backoffBaseMs: 1000 },
    status: 'IDLE',
    version: '1.0.0'
  },
  {
    id: 'mkt-13',
    name: 'Distribution Strategist',
    category: 'MARKETING_GROWTH',
    role: 'Coordinates multi-channel broadcasting, cross-posting schedules, and timing optimization for Indian user habits.',
    systemInstruction: `You are the ${'Distribution Strategist'}, operating within an autonomous AI Marketing Organization. Your core role is: ${'Coordinates multi-channel broadcasting, cross-posting schedules, and timing optimization for Indian user habits.'}. Always deliver evidence-backed, actionable, and structured outputs tailored to the target market and business goals.`,
    capabilities: ['data_analysis', 'structured_synthesis', 'domain_reasoning'],
    allowedTools: ['database_read', 'database_write', 'evidence_retrieval', 'analytics_query'],
    inputSchema: { type: 'object', properties: { context: { type: 'string' }, goal: { type: 'object' } }, required: ['context'] },
    outputSchema: { type: 'object', properties: { findings: { type: 'array' }, recommendation: { type: 'string' }, confidence: { type: 'number' } }, required: ['recommendation', 'confidence'] },
    confidenceThreshold: 0.8,
    taskPermissions: ['READ_ONLY', 'PROPOSE_DECISION'],
    costBudget: 500,
    executionLimits: { maxRetries: 3, timeoutSeconds: 60, maxTokens: 4096 },
    performanceMetrics: {
      completionRate: 0.98,
      approvalRate: 0.95,
      avgLatencyMs: 1200,
      totalExecutions: 24,
      successfulExecutions: 23,
      failedExecutions: 1,
      costIncurredINR: 4.5
    },
    memoryScope: 'BUSINESS',
    evaluationCriteria: ['Factual accuracy', 'Strategic relevance', 'Actionability', 'Compliance safety'],
    failurePolicy: 'RETRY',
    retryPolicy: { maxAttempts: 3, backoffBaseMs: 1000 },
    status: 'IDLE',
    version: '1.0.0'
  },
  {
    id: 'mkt-14',
    name: 'Partnership Strategist',
    category: 'MARKETING_GROWTH',
    role: 'Develops co-marketing, cross-promotions, and local business alliance opportunities.',
    systemInstruction: `You are the ${'Partnership Strategist'}, operating within an autonomous AI Marketing Organization. Your core role is: ${'Develops co-marketing, cross-promotions, and local business alliance opportunities.'}. Always deliver evidence-backed, actionable, and structured outputs tailored to the target market and business goals.`,
    capabilities: ['data_analysis', 'structured_synthesis', 'domain_reasoning'],
    allowedTools: ['database_read', 'database_write', 'evidence_retrieval', 'analytics_query'],
    inputSchema: { type: 'object', properties: { context: { type: 'string' }, goal: { type: 'object' } }, required: ['context'] },
    outputSchema: { type: 'object', properties: { findings: { type: 'array' }, recommendation: { type: 'string' }, confidence: { type: 'number' } }, required: ['recommendation', 'confidence'] },
    confidenceThreshold: 0.8,
    taskPermissions: ['READ_ONLY', 'PROPOSE_DECISION'],
    costBudget: 500,
    executionLimits: { maxRetries: 3, timeoutSeconds: 60, maxTokens: 4096 },
    performanceMetrics: {
      completionRate: 0.98,
      approvalRate: 0.95,
      avgLatencyMs: 1200,
      totalExecutions: 24,
      successfulExecutions: 23,
      failedExecutions: 1,
      costIncurredINR: 4.5
    },
    memoryScope: 'BUSINESS',
    evaluationCriteria: ['Factual accuracy', 'Strategic relevance', 'Actionability', 'Compliance safety'],
    failurePolicy: 'RETRY',
    retryPolicy: { maxAttempts: 3, backoffBaseMs: 1000 },
    status: 'IDLE',
    version: '1.0.0'
  },
  {
    id: 'mkt-15',
    name: 'Influencer Strategist',
    category: 'MARKETING_GROWTH',
    role: 'Identifies and coordinates micro-influencer partnerships and local creator endorsements.',
    systemInstruction: `You are the ${'Influencer Strategist'}, operating within an autonomous AI Marketing Organization. Your core role is: ${'Identifies and coordinates micro-influencer partnerships and local creator endorsements.'}. Always deliver evidence-backed, actionable, and structured outputs tailored to the target market and business goals.`,
    capabilities: ['data_analysis', 'structured_synthesis', 'domain_reasoning'],
    allowedTools: ['database_read', 'database_write', 'evidence_retrieval', 'analytics_query'],
    inputSchema: { type: 'object', properties: { context: { type: 'string' }, goal: { type: 'object' } }, required: ['context'] },
    outputSchema: { type: 'object', properties: { findings: { type: 'array' }, recommendation: { type: 'string' }, confidence: { type: 'number' } }, required: ['recommendation', 'confidence'] },
    confidenceThreshold: 0.8,
    taskPermissions: ['READ_ONLY', 'PROPOSE_DECISION'],
    costBudget: 500,
    executionLimits: { maxRetries: 3, timeoutSeconds: 60, maxTokens: 4096 },
    performanceMetrics: {
      completionRate: 0.98,
      approvalRate: 0.95,
      avgLatencyMs: 1200,
      totalExecutions: 24,
      successfulExecutions: 23,
      failedExecutions: 1,
      costIncurredINR: 4.5
    },
    memoryScope: 'BUSINESS',
    evaluationCriteria: ['Factual accuracy', 'Strategic relevance', 'Actionability', 'Compliance safety'],
    failurePolicy: 'RETRY',
    retryPolicy: { maxAttempts: 3, backoffBaseMs: 1000 },
    status: 'IDLE',
    version: '1.0.0'
  },
  {
    id: 'mkt-16',
    name: 'Community Strategist',
    category: 'MARKETING_GROWTH',
    role: 'Cultivates loyal customer communities, VIP circles, and user-generated content initiatives.',
    systemInstruction: `You are the ${'Community Strategist'}, operating within an autonomous AI Marketing Organization. Your core role is: ${'Cultivates loyal customer communities, VIP circles, and user-generated content initiatives.'}. Always deliver evidence-backed, actionable, and structured outputs tailored to the target market and business goals.`,
    capabilities: ['data_analysis', 'structured_synthesis', 'domain_reasoning'],
    allowedTools: ['database_read', 'database_write', 'evidence_retrieval', 'analytics_query'],
    inputSchema: { type: 'object', properties: { context: { type: 'string' }, goal: { type: 'object' } }, required: ['context'] },
    outputSchema: { type: 'object', properties: { findings: { type: 'array' }, recommendation: { type: 'string' }, confidence: { type: 'number' } }, required: ['recommendation', 'confidence'] },
    confidenceThreshold: 0.8,
    taskPermissions: ['READ_ONLY', 'PROPOSE_DECISION'],
    costBudget: 500,
    executionLimits: { maxRetries: 3, timeoutSeconds: 60, maxTokens: 4096 },
    performanceMetrics: {
      completionRate: 0.98,
      approvalRate: 0.95,
      avgLatencyMs: 1200,
      totalExecutions: 24,
      successfulExecutions: 23,
      failedExecutions: 1,
      costIncurredINR: 4.5
    },
    memoryScope: 'BUSINESS',
    evaluationCriteria: ['Factual accuracy', 'Strategic relevance', 'Actionability', 'Compliance safety'],
    failurePolicy: 'RETRY',
    retryPolicy: { maxAttempts: 3, backoffBaseMs: 1000 },
    status: 'IDLE',
    version: '1.0.0'
  },
  {
    id: 'mkt-17',
    name: 'Launch Strategist',
    category: 'MARKETING_GROWTH',
    role: 'Executes high-impact launch sequences for new clinics, seasonal services, and flagship packages.',
    systemInstruction: `You are the ${'Launch Strategist'}, operating within an autonomous AI Marketing Organization. Your core role is: ${'Executes high-impact launch sequences for new clinics, seasonal services, and flagship packages.'}. Always deliver evidence-backed, actionable, and structured outputs tailored to the target market and business goals.`,
    capabilities: ['data_analysis', 'structured_synthesis', 'domain_reasoning'],
    allowedTools: ['database_read', 'database_write', 'evidence_retrieval', 'analytics_query'],
    inputSchema: { type: 'object', properties: { context: { type: 'string' }, goal: { type: 'object' } }, required: ['context'] },
    outputSchema: { type: 'object', properties: { findings: { type: 'array' }, recommendation: { type: 'string' }, confidence: { type: 'number' } }, required: ['recommendation', 'confidence'] },
    confidenceThreshold: 0.8,
    taskPermissions: ['READ_ONLY', 'PROPOSE_DECISION'],
    costBudget: 500,
    executionLimits: { maxRetries: 3, timeoutSeconds: 60, maxTokens: 4096 },
    performanceMetrics: {
      completionRate: 0.98,
      approvalRate: 0.95,
      avgLatencyMs: 1200,
      totalExecutions: 24,
      successfulExecutions: 23,
      failedExecutions: 1,
      costIncurredINR: 4.5
    },
    memoryScope: 'BUSINESS',
    evaluationCriteria: ['Factual accuracy', 'Strategic relevance', 'Actionability', 'Compliance safety'],
    failurePolicy: 'RETRY',
    retryPolicy: { maxAttempts: 3, backoffBaseMs: 1000 },
    status: 'IDLE',
    version: '1.0.0'
  },
  {
    id: 'mkt-18',
    name: 'Retention Strategist',
    category: 'MARKETING_GROWTH',
    role: 'Improves customer lifetime value, rebooking rates, preventive care recall, and loyalty program participation.',
    systemInstruction: `You are the ${'Retention Strategist'}, operating within an autonomous AI Marketing Organization. Your core role is: ${'Improves customer lifetime value, rebooking rates, preventive care recall, and loyalty program participation.'}. Always deliver evidence-backed, actionable, and structured outputs tailored to the target market and business goals.`,
    capabilities: ['data_analysis', 'structured_synthesis', 'domain_reasoning'],
    allowedTools: ['database_read', 'database_write', 'evidence_retrieval', 'analytics_query'],
    inputSchema: { type: 'object', properties: { context: { type: 'string' }, goal: { type: 'object' } }, required: ['context'] },
    outputSchema: { type: 'object', properties: { findings: { type: 'array' }, recommendation: { type: 'string' }, confidence: { type: 'number' } }, required: ['recommendation', 'confidence'] },
    confidenceThreshold: 0.8,
    taskPermissions: ['READ_ONLY', 'PROPOSE_DECISION'],
    costBudget: 500,
    executionLimits: { maxRetries: 3, timeoutSeconds: 60, maxTokens: 4096 },
    performanceMetrics: {
      completionRate: 0.98,
      approvalRate: 0.95,
      avgLatencyMs: 1200,
      totalExecutions: 24,
      successfulExecutions: 23,
      failedExecutions: 1,
      costIncurredINR: 4.5
    },
    memoryScope: 'BUSINESS',
    evaluationCriteria: ['Factual accuracy', 'Strategic relevance', 'Actionability', 'Compliance safety'],
    failurePolicy: 'RETRY',
    retryPolicy: { maxAttempts: 3, backoffBaseMs: 1000 },
    status: 'IDLE',
    version: '1.0.0'
  },
  {
    id: 'mkt-19',
    name: 'Experiment Strategist',
    category: 'MARKETING_GROWTH',
    role: 'Formulates scientifically rigorous marketing hypotheses, A/B test variations, and statistical validation parameters.',
    systemInstruction: `You are the ${'Experiment Strategist'}, operating within an autonomous AI Marketing Organization. Your core role is: ${'Formulates scientifically rigorous marketing hypotheses, A/B test variations, and statistical validation parameters.'}. Always deliver evidence-backed, actionable, and structured outputs tailored to the target market and business goals.`,
    capabilities: ['data_analysis', 'structured_synthesis', 'domain_reasoning'],
    allowedTools: ['database_read', 'database_write', 'evidence_retrieval', 'analytics_query'],
    inputSchema: { type: 'object', properties: { context: { type: 'string' }, goal: { type: 'object' } }, required: ['context'] },
    outputSchema: { type: 'object', properties: { findings: { type: 'array' }, recommendation: { type: 'string' }, confidence: { type: 'number' } }, required: ['recommendation', 'confidence'] },
    confidenceThreshold: 0.8,
    taskPermissions: ['READ_ONLY', 'PROPOSE_DECISION'],
    costBudget: 500,
    executionLimits: { maxRetries: 3, timeoutSeconds: 60, maxTokens: 4096 },
    performanceMetrics: {
      completionRate: 0.98,
      approvalRate: 0.95,
      avgLatencyMs: 1200,
      totalExecutions: 24,
      successfulExecutions: 23,
      failedExecutions: 1,
      costIncurredINR: 4.5
    },
    memoryScope: 'BUSINESS',
    evaluationCriteria: ['Factual accuracy', 'Strategic relevance', 'Actionability', 'Compliance safety'],
    failurePolicy: 'RETRY',
    retryPolicy: { maxAttempts: 3, backoffBaseMs: 1000 },
    status: 'IDLE',
    version: '1.0.0'
  },
  {
    id: 'mkt-20',
    name: 'Growth Optimizer',
    category: 'MARKETING_GROWTH',
    role: 'Monitors real-time performance to reallocate budgets to highest-converting channels and kill underperforming campaigns.',
    systemInstruction: `You are the ${'Growth Optimizer'}, operating within an autonomous AI Marketing Organization. Your core role is: ${'Monitors real-time performance to reallocate budgets to highest-converting channels and kill underperforming campaigns.'}. Always deliver evidence-backed, actionable, and structured outputs tailored to the target market and business goals.`,
    capabilities: ['data_analysis', 'structured_synthesis', 'domain_reasoning'],
    allowedTools: ['database_read', 'database_write', 'evidence_retrieval', 'analytics_query'],
    inputSchema: { type: 'object', properties: { context: { type: 'string' }, goal: { type: 'object' } }, required: ['context'] },
    outputSchema: { type: 'object', properties: { findings: { type: 'array' }, recommendation: { type: 'string' }, confidence: { type: 'number' } }, required: ['recommendation', 'confidence'] },
    confidenceThreshold: 0.8,
    taskPermissions: ['READ_ONLY', 'PROPOSE_DECISION'],
    costBudget: 500,
    executionLimits: { maxRetries: 3, timeoutSeconds: 60, maxTokens: 4096 },
    performanceMetrics: {
      completionRate: 0.98,
      approvalRate: 0.95,
      avgLatencyMs: 1200,
      totalExecutions: 24,
      successfulExecutions: 23,
      failedExecutions: 1,
      costIncurredINR: 4.5
    },
    memoryScope: 'BUSINESS',
    evaluationCriteria: ['Factual accuracy', 'Strategic relevance', 'Actionability', 'Compliance safety'],
    failurePolicy: 'RETRY',
    retryPolicy: { maxAttempts: 3, backoffBaseMs: 1000 },
    status: 'IDLE',
    version: '1.0.0'
  }
  ,
  // Analytics & Learning (20)
  {
    id: 'anl-01',
    name: 'Data Analyst',
    category: 'ANALYTICS_LEARNING',
    role: 'Cleans, validates, and normalizes incoming telemetry across web, social, WhatsApp, and CRM channels.',
    systemInstruction: `You are the ${'Data Analyst'}, operating within an autonomous AI Marketing Organization. Your core role is: ${'Cleans, validates, and normalizes incoming telemetry across web, social, WhatsApp, and CRM channels.'}. Always deliver evidence-backed, actionable, and structured outputs tailored to the target market and business goals.`,
    capabilities: ['data_analysis', 'structured_synthesis', 'domain_reasoning'],
    allowedTools: ['database_read', 'database_write', 'evidence_retrieval', 'analytics_query'],
    inputSchema: { type: 'object', properties: { context: { type: 'string' }, goal: { type: 'object' } }, required: ['context'] },
    outputSchema: { type: 'object', properties: { findings: { type: 'array' }, recommendation: { type: 'string' }, confidence: { type: 'number' } }, required: ['recommendation', 'confidence'] },
    confidenceThreshold: 0.8,
    taskPermissions: ['READ_ONLY', 'PROPOSE_DECISION'],
    costBudget: 500,
    executionLimits: { maxRetries: 3, timeoutSeconds: 60, maxTokens: 4096 },
    performanceMetrics: {
      completionRate: 0.98,
      approvalRate: 0.95,
      avgLatencyMs: 1200,
      totalExecutions: 24,
      successfulExecutions: 23,
      failedExecutions: 1,
      costIncurredINR: 4.5
    },
    memoryScope: 'SYSTEM',
    evaluationCriteria: ['Factual accuracy', 'Strategic relevance', 'Actionability', 'Compliance safety'],
    failurePolicy: 'RETRY',
    retryPolicy: { maxAttempts: 3, backoffBaseMs: 1000 },
    status: 'IDLE',
    version: '1.0.0'
  },
  {
    id: 'anl-02',
    name: 'Marketing Analyst',
    category: 'ANALYTICS_LEARNING',
    role: 'Computes high-level marketing efficiency ratios (MER), customer acquisition costs (CAC), and pipeline velocity.',
    systemInstruction: `You are the ${'Marketing Analyst'}, operating within an autonomous AI Marketing Organization. Your core role is: ${'Computes high-level marketing efficiency ratios (MER), customer acquisition costs (CAC), and pipeline velocity.'}. Always deliver evidence-backed, actionable, and structured outputs tailored to the target market and business goals.`,
    capabilities: ['data_analysis', 'structured_synthesis', 'domain_reasoning'],
    allowedTools: ['database_read', 'database_write', 'evidence_retrieval', 'analytics_query'],
    inputSchema: { type: 'object', properties: { context: { type: 'string' }, goal: { type: 'object' } }, required: ['context'] },
    outputSchema: { type: 'object', properties: { findings: { type: 'array' }, recommendation: { type: 'string' }, confidence: { type: 'number' } }, required: ['recommendation', 'confidence'] },
    confidenceThreshold: 0.8,
    taskPermissions: ['READ_ONLY', 'PROPOSE_DECISION'],
    costBudget: 500,
    executionLimits: { maxRetries: 3, timeoutSeconds: 60, maxTokens: 4096 },
    performanceMetrics: {
      completionRate: 0.98,
      approvalRate: 0.95,
      avgLatencyMs: 1200,
      totalExecutions: 24,
      successfulExecutions: 23,
      failedExecutions: 1,
      costIncurredINR: 4.5
    },
    memoryScope: 'SYSTEM',
    evaluationCriteria: ['Factual accuracy', 'Strategic relevance', 'Actionability', 'Compliance safety'],
    failurePolicy: 'RETRY',
    retryPolicy: { maxAttempts: 3, backoffBaseMs: 1000 },
    status: 'IDLE',
    version: '1.0.0'
  },
  {
    id: 'anl-03',
    name: 'Content Analyst',
    category: 'ANALYTICS_LEARNING',
    role: 'Evaluates content piece performance, hook retention rates, and content-assisted qualified lead volume.',
    systemInstruction: `You are the ${'Content Analyst'}, operating within an autonomous AI Marketing Organization. Your core role is: ${'Evaluates content piece performance, hook retention rates, and content-assisted qualified lead volume.'}. Always deliver evidence-backed, actionable, and structured outputs tailored to the target market and business goals.`,
    capabilities: ['data_analysis', 'structured_synthesis', 'domain_reasoning'],
    allowedTools: ['database_read', 'database_write', 'evidence_retrieval', 'analytics_query'],
    inputSchema: { type: 'object', properties: { context: { type: 'string' }, goal: { type: 'object' } }, required: ['context'] },
    outputSchema: { type: 'object', properties: { findings: { type: 'array' }, recommendation: { type: 'string' }, confidence: { type: 'number' } }, required: ['recommendation', 'confidence'] },
    confidenceThreshold: 0.8,
    taskPermissions: ['READ_ONLY', 'PROPOSE_DECISION'],
    costBudget: 500,
    executionLimits: { maxRetries: 3, timeoutSeconds: 60, maxTokens: 4096 },
    performanceMetrics: {
      completionRate: 0.98,
      approvalRate: 0.95,
      avgLatencyMs: 1200,
      totalExecutions: 24,
      successfulExecutions: 23,
      failedExecutions: 1,
      costIncurredINR: 4.5
    },
    memoryScope: 'SYSTEM',
    evaluationCriteria: ['Factual accuracy', 'Strategic relevance', 'Actionability', 'Compliance safety'],
    failurePolicy: 'RETRY',
    retryPolicy: { maxAttempts: 3, backoffBaseMs: 1000 },
    status: 'IDLE',
    version: '1.0.0'
  },
  {
    id: 'anl-04',
    name: 'SEO Analyst',
    category: 'ANALYTICS_LEARNING',
    role: 'Tracks keyword rankings, organic impressions, click-through rates, and Google Map pack local visibility.',
    systemInstruction: `You are the ${'SEO Analyst'}, operating within an autonomous AI Marketing Organization. Your core role is: ${'Tracks keyword rankings, organic impressions, click-through rates, and Google Map pack local visibility.'}. Always deliver evidence-backed, actionable, and structured outputs tailored to the target market and business goals.`,
    capabilities: ['data_analysis', 'structured_synthesis', 'domain_reasoning'],
    allowedTools: ['database_read', 'database_write', 'evidence_retrieval', 'analytics_query'],
    inputSchema: { type: 'object', properties: { context: { type: 'string' }, goal: { type: 'object' } }, required: ['context'] },
    outputSchema: { type: 'object', properties: { findings: { type: 'array' }, recommendation: { type: 'string' }, confidence: { type: 'number' } }, required: ['recommendation', 'confidence'] },
    confidenceThreshold: 0.8,
    taskPermissions: ['READ_ONLY', 'PROPOSE_DECISION'],
    costBudget: 500,
    executionLimits: { maxRetries: 3, timeoutSeconds: 60, maxTokens: 4096 },
    performanceMetrics: {
      completionRate: 0.98,
      approvalRate: 0.95,
      avgLatencyMs: 1200,
      totalExecutions: 24,
      successfulExecutions: 23,
      failedExecutions: 1,
      costIncurredINR: 4.5
    },
    memoryScope: 'SYSTEM',
    evaluationCriteria: ['Factual accuracy', 'Strategic relevance', 'Actionability', 'Compliance safety'],
    failurePolicy: 'RETRY',
    retryPolicy: { maxAttempts: 3, backoffBaseMs: 1000 },
    status: 'IDLE',
    version: '1.0.0'
  },
  {
    id: 'anl-05',
    name: 'Social Analyst',
    category: 'ANALYTICS_LEARNING',
    role: 'Assesses engagement rates, follower growth velocity, video completion rates, and share ratios across channels.',
    systemInstruction: `You are the ${'Social Analyst'}, operating within an autonomous AI Marketing Organization. Your core role is: ${'Assesses engagement rates, follower growth velocity, video completion rates, and share ratios across channels.'}. Always deliver evidence-backed, actionable, and structured outputs tailored to the target market and business goals.`,
    capabilities: ['data_analysis', 'structured_synthesis', 'domain_reasoning'],
    allowedTools: ['database_read', 'database_write', 'evidence_retrieval', 'analytics_query'],
    inputSchema: { type: 'object', properties: { context: { type: 'string' }, goal: { type: 'object' } }, required: ['context'] },
    outputSchema: { type: 'object', properties: { findings: { type: 'array' }, recommendation: { type: 'string' }, confidence: { type: 'number' } }, required: ['recommendation', 'confidence'] },
    confidenceThreshold: 0.8,
    taskPermissions: ['READ_ONLY', 'PROPOSE_DECISION'],
    costBudget: 500,
    executionLimits: { maxRetries: 3, timeoutSeconds: 60, maxTokens: 4096 },
    performanceMetrics: {
      completionRate: 0.98,
      approvalRate: 0.95,
      avgLatencyMs: 1200,
      totalExecutions: 24,
      successfulExecutions: 23,
      failedExecutions: 1,
      costIncurredINR: 4.5
    },
    memoryScope: 'SYSTEM',
    evaluationCriteria: ['Factual accuracy', 'Strategic relevance', 'Actionability', 'Compliance safety'],
    failurePolicy: 'RETRY',
    retryPolicy: { maxAttempts: 3, backoffBaseMs: 1000 },
    status: 'IDLE',
    version: '1.0.0'
  },
  {
    id: 'anl-06',
    name: 'Campaign Analyst',
    category: 'ANALYTICS_LEARNING',
    role: 'Audits individual campaign efficiency against target KPIs, flight budgets, and qualified lead targets.',
    systemInstruction: `You are the ${'Campaign Analyst'}, operating within an autonomous AI Marketing Organization. Your core role is: ${'Audits individual campaign efficiency against target KPIs, flight budgets, and qualified lead targets.'}. Always deliver evidence-backed, actionable, and structured outputs tailored to the target market and business goals.`,
    capabilities: ['data_analysis', 'structured_synthesis', 'domain_reasoning'],
    allowedTools: ['database_read', 'database_write', 'evidence_retrieval', 'analytics_query'],
    inputSchema: { type: 'object', properties: { context: { type: 'string' }, goal: { type: 'object' } }, required: ['context'] },
    outputSchema: { type: 'object', properties: { findings: { type: 'array' }, recommendation: { type: 'string' }, confidence: { type: 'number' } }, required: ['recommendation', 'confidence'] },
    confidenceThreshold: 0.8,
    taskPermissions: ['READ_ONLY', 'PROPOSE_DECISION'],
    costBudget: 500,
    executionLimits: { maxRetries: 3, timeoutSeconds: 60, maxTokens: 4096 },
    performanceMetrics: {
      completionRate: 0.98,
      approvalRate: 0.95,
      avgLatencyMs: 1200,
      totalExecutions: 24,
      successfulExecutions: 23,
      failedExecutions: 1,
      costIncurredINR: 4.5
    },
    memoryScope: 'SYSTEM',
    evaluationCriteria: ['Factual accuracy', 'Strategic relevance', 'Actionability', 'Compliance safety'],
    failurePolicy: 'RETRY',
    retryPolicy: { maxAttempts: 3, backoffBaseMs: 1000 },
    status: 'IDLE',
    version: '1.0.0'
  },
  {
    id: 'anl-07',
    name: 'Funnel Analyst',
    category: 'ANALYTICS_LEARNING',
    role: 'Identifies drop-off bottlenecks between impression, click, inquiry, qualified lead, and consultation.',
    systemInstruction: `You are the ${'Funnel Analyst'}, operating within an autonomous AI Marketing Organization. Your core role is: ${'Identifies drop-off bottlenecks between impression, click, inquiry, qualified lead, and consultation.'}. Always deliver evidence-backed, actionable, and structured outputs tailored to the target market and business goals.`,
    capabilities: ['data_analysis', 'structured_synthesis', 'domain_reasoning'],
    allowedTools: ['database_read', 'database_write', 'evidence_retrieval', 'analytics_query'],
    inputSchema: { type: 'object', properties: { context: { type: 'string' }, goal: { type: 'object' } }, required: ['context'] },
    outputSchema: { type: 'object', properties: { findings: { type: 'array' }, recommendation: { type: 'string' }, confidence: { type: 'number' } }, required: ['recommendation', 'confidence'] },
    confidenceThreshold: 0.8,
    taskPermissions: ['READ_ONLY', 'PROPOSE_DECISION'],
    costBudget: 500,
    executionLimits: { maxRetries: 3, timeoutSeconds: 60, maxTokens: 4096 },
    performanceMetrics: {
      completionRate: 0.98,
      approvalRate: 0.95,
      avgLatencyMs: 1200,
      totalExecutions: 24,
      successfulExecutions: 23,
      failedExecutions: 1,
      costIncurredINR: 4.5
    },
    memoryScope: 'SYSTEM',
    evaluationCriteria: ['Factual accuracy', 'Strategic relevance', 'Actionability', 'Compliance safety'],
    failurePolicy: 'RETRY',
    retryPolicy: { maxAttempts: 3, backoffBaseMs: 1000 },
    status: 'IDLE',
    version: '1.0.0'
  },
  {
    id: 'anl-08',
    name: 'Conversion Analyst',
    category: 'ANALYTICS_LEARNING',
    role: 'Performs form-level and checkout-level conversion audits to minimize user friction.',
    systemInstruction: `You are the ${'Conversion Analyst'}, operating within an autonomous AI Marketing Organization. Your core role is: ${'Performs form-level and checkout-level conversion audits to minimize user friction.'}. Always deliver evidence-backed, actionable, and structured outputs tailored to the target market and business goals.`,
    capabilities: ['data_analysis', 'structured_synthesis', 'domain_reasoning'],
    allowedTools: ['database_read', 'database_write', 'evidence_retrieval', 'analytics_query'],
    inputSchema: { type: 'object', properties: { context: { type: 'string' }, goal: { type: 'object' } }, required: ['context'] },
    outputSchema: { type: 'object', properties: { findings: { type: 'array' }, recommendation: { type: 'string' }, confidence: { type: 'number' } }, required: ['recommendation', 'confidence'] },
    confidenceThreshold: 0.8,
    taskPermissions: ['READ_ONLY', 'PROPOSE_DECISION'],
    costBudget: 500,
    executionLimits: { maxRetries: 3, timeoutSeconds: 60, maxTokens: 4096 },
    performanceMetrics: {
      completionRate: 0.98,
      approvalRate: 0.95,
      avgLatencyMs: 1200,
      totalExecutions: 24,
      successfulExecutions: 23,
      failedExecutions: 1,
      costIncurredINR: 4.5
    },
    memoryScope: 'SYSTEM',
    evaluationCriteria: ['Factual accuracy', 'Strategic relevance', 'Actionability', 'Compliance safety'],
    failurePolicy: 'RETRY',
    retryPolicy: { maxAttempts: 3, backoffBaseMs: 1000 },
    status: 'IDLE',
    version: '1.0.0'
  },
  {
    id: 'anl-09',
    name: 'Revenue Analyst',
    category: 'ANALYTICS_LEARNING',
    role: 'Reconciles marketing spend with realized revenue, average order value (AOV), and customer profitability in INR.',
    systemInstruction: `You are the ${'Revenue Analyst'}, operating within an autonomous AI Marketing Organization. Your core role is: ${'Reconciles marketing spend with realized revenue, average order value (AOV), and customer profitability in INR.'}. Always deliver evidence-backed, actionable, and structured outputs tailored to the target market and business goals.`,
    capabilities: ['data_analysis', 'structured_synthesis', 'domain_reasoning'],
    allowedTools: ['database_read', 'database_write', 'evidence_retrieval', 'analytics_query'],
    inputSchema: { type: 'object', properties: { context: { type: 'string' }, goal: { type: 'object' } }, required: ['context'] },
    outputSchema: { type: 'object', properties: { findings: { type: 'array' }, recommendation: { type: 'string' }, confidence: { type: 'number' } }, required: ['recommendation', 'confidence'] },
    confidenceThreshold: 0.8,
    taskPermissions: ['READ_ONLY', 'PROPOSE_DECISION'],
    costBudget: 500,
    executionLimits: { maxRetries: 3, timeoutSeconds: 60, maxTokens: 4096 },
    performanceMetrics: {
      completionRate: 0.98,
      approvalRate: 0.95,
      avgLatencyMs: 1200,
      totalExecutions: 24,
      successfulExecutions: 23,
      failedExecutions: 1,
      costIncurredINR: 4.5
    },
    memoryScope: 'SYSTEM',
    evaluationCriteria: ['Factual accuracy', 'Strategic relevance', 'Actionability', 'Compliance safety'],
    failurePolicy: 'RETRY',
    retryPolicy: { maxAttempts: 3, backoffBaseMs: 1000 },
    status: 'IDLE',
    version: '1.0.0'
  },
  {
    id: 'anl-10',
    name: 'Attribution Analyst',
    category: 'ANALYTICS_LEARNING',
    role: 'Executes multi-touch attribution (First-Touch, Last-Touch, Linear, Assisted) to quantify channel contribution.',
    systemInstruction: `You are the ${'Attribution Analyst'}, operating within an autonomous AI Marketing Organization. Your core role is: ${'Executes multi-touch attribution (First-Touch, Last-Touch, Linear, Assisted) to quantify channel contribution.'}. Always deliver evidence-backed, actionable, and structured outputs tailored to the target market and business goals.`,
    capabilities: ['data_analysis', 'structured_synthesis', 'domain_reasoning'],
    allowedTools: ['database_read', 'database_write', 'evidence_retrieval', 'analytics_query'],
    inputSchema: { type: 'object', properties: { context: { type: 'string' }, goal: { type: 'object' } }, required: ['context'] },
    outputSchema: { type: 'object', properties: { findings: { type: 'array' }, recommendation: { type: 'string' }, confidence: { type: 'number' } }, required: ['recommendation', 'confidence'] },
    confidenceThreshold: 0.8,
    taskPermissions: ['READ_ONLY', 'PROPOSE_DECISION'],
    costBudget: 500,
    executionLimits: { maxRetries: 3, timeoutSeconds: 60, maxTokens: 4096 },
    performanceMetrics: {
      completionRate: 0.98,
      approvalRate: 0.95,
      avgLatencyMs: 1200,
      totalExecutions: 24,
      successfulExecutions: 23,
      failedExecutions: 1,
      costIncurredINR: 4.5
    },
    memoryScope: 'SYSTEM',
    evaluationCriteria: ['Factual accuracy', 'Strategic relevance', 'Actionability', 'Compliance safety'],
    failurePolicy: 'RETRY',
    retryPolicy: { maxAttempts: 3, backoffBaseMs: 1000 },
    status: 'IDLE',
    version: '1.0.0'
  },
  {
    id: 'anl-11',
    name: 'Cohort Analyst',
    category: 'ANALYTICS_LEARNING',
    role: 'Analyzes patient and customer retention curves, repeat treatment cycles, and long-term cohort value.',
    systemInstruction: `You are the ${'Cohort Analyst'}, operating within an autonomous AI Marketing Organization. Your core role is: ${'Analyzes patient and customer retention curves, repeat treatment cycles, and long-term cohort value.'}. Always deliver evidence-backed, actionable, and structured outputs tailored to the target market and business goals.`,
    capabilities: ['data_analysis', 'structured_synthesis', 'domain_reasoning'],
    allowedTools: ['database_read', 'database_write', 'evidence_retrieval', 'analytics_query'],
    inputSchema: { type: 'object', properties: { context: { type: 'string' }, goal: { type: 'object' } }, required: ['context'] },
    outputSchema: { type: 'object', properties: { findings: { type: 'array' }, recommendation: { type: 'string' }, confidence: { type: 'number' } }, required: ['recommendation', 'confidence'] },
    confidenceThreshold: 0.8,
    taskPermissions: ['READ_ONLY', 'PROPOSE_DECISION'],
    costBudget: 500,
    executionLimits: { maxRetries: 3, timeoutSeconds: 60, maxTokens: 4096 },
    performanceMetrics: {
      completionRate: 0.98,
      approvalRate: 0.95,
      avgLatencyMs: 1200,
      totalExecutions: 24,
      successfulExecutions: 23,
      failedExecutions: 1,
      costIncurredINR: 4.5
    },
    memoryScope: 'SYSTEM',
    evaluationCriteria: ['Factual accuracy', 'Strategic relevance', 'Actionability', 'Compliance safety'],
    failurePolicy: 'RETRY',
    retryPolicy: { maxAttempts: 3, backoffBaseMs: 1000 },
    status: 'IDLE',
    version: '1.0.0'
  },
  {
    id: 'anl-12',
    name: 'Customer Analyst',
    category: 'ANALYTICS_LEARNING',
    role: 'Profiles high-value customer characteristics to refine ICP targeting and personalized messaging.',
    systemInstruction: `You are the ${'Customer Analyst'}, operating within an autonomous AI Marketing Organization. Your core role is: ${'Profiles high-value customer characteristics to refine ICP targeting and personalized messaging.'}. Always deliver evidence-backed, actionable, and structured outputs tailored to the target market and business goals.`,
    capabilities: ['data_analysis', 'structured_synthesis', 'domain_reasoning'],
    allowedTools: ['database_read', 'database_write', 'evidence_retrieval', 'analytics_query'],
    inputSchema: { type: 'object', properties: { context: { type: 'string' }, goal: { type: 'object' } }, required: ['context'] },
    outputSchema: { type: 'object', properties: { findings: { type: 'array' }, recommendation: { type: 'string' }, confidence: { type: 'number' } }, required: ['recommendation', 'confidence'] },
    confidenceThreshold: 0.8,
    taskPermissions: ['READ_ONLY', 'PROPOSE_DECISION'],
    costBudget: 500,
    executionLimits: { maxRetries: 3, timeoutSeconds: 60, maxTokens: 4096 },
    performanceMetrics: {
      completionRate: 0.98,
      approvalRate: 0.95,
      avgLatencyMs: 1200,
      totalExecutions: 24,
      successfulExecutions: 23,
      failedExecutions: 1,
      costIncurredINR: 4.5
    },
    memoryScope: 'SYSTEM',
    evaluationCriteria: ['Factual accuracy', 'Strategic relevance', 'Actionability', 'Compliance safety'],
    failurePolicy: 'RETRY',
    retryPolicy: { maxAttempts: 3, backoffBaseMs: 1000 },
    status: 'IDLE',
    version: '1.0.0'
  },
  {
    id: 'anl-13',
    name: 'A/B Testing Agent',
    category: 'ANALYTICS_LEARNING',
    role: 'Calculates statistical significance, p-values, and conversion deltas for running marketing experiments.',
    systemInstruction: `You are the ${'A/B Testing Agent'}, operating within an autonomous AI Marketing Organization. Your core role is: ${'Calculates statistical significance, p-values, and conversion deltas for running marketing experiments.'}. Always deliver evidence-backed, actionable, and structured outputs tailored to the target market and business goals.`,
    capabilities: ['data_analysis', 'structured_synthesis', 'domain_reasoning'],
    allowedTools: ['database_read', 'database_write', 'evidence_retrieval', 'analytics_query'],
    inputSchema: { type: 'object', properties: { context: { type: 'string' }, goal: { type: 'object' } }, required: ['context'] },
    outputSchema: { type: 'object', properties: { findings: { type: 'array' }, recommendation: { type: 'string' }, confidence: { type: 'number' } }, required: ['recommendation', 'confidence'] },
    confidenceThreshold: 0.8,
    taskPermissions: ['READ_ONLY', 'PROPOSE_DECISION'],
    costBudget: 500,
    executionLimits: { maxRetries: 3, timeoutSeconds: 60, maxTokens: 4096 },
    performanceMetrics: {
      completionRate: 0.98,
      approvalRate: 0.95,
      avgLatencyMs: 1200,
      totalExecutions: 24,
      successfulExecutions: 23,
      failedExecutions: 1,
      costIncurredINR: 4.5
    },
    memoryScope: 'SYSTEM',
    evaluationCriteria: ['Factual accuracy', 'Strategic relevance', 'Actionability', 'Compliance safety'],
    failurePolicy: 'RETRY',
    retryPolicy: { maxAttempts: 3, backoffBaseMs: 1000 },
    status: 'IDLE',
    version: '1.0.0'
  },
  {
    id: 'anl-14',
    name: 'Anomaly Detection Agent',
    category: 'ANALYTICS_LEARNING',
    role: 'Detects unexpected traffic spikes, CPA anomalies, broken lead forms, and channel performance crashes.',
    systemInstruction: `You are the ${'Anomaly Detection Agent'}, operating within an autonomous AI Marketing Organization. Your core role is: ${'Detects unexpected traffic spikes, CPA anomalies, broken lead forms, and channel performance crashes.'}. Always deliver evidence-backed, actionable, and structured outputs tailored to the target market and business goals.`,
    capabilities: ['data_analysis', 'structured_synthesis', 'domain_reasoning'],
    allowedTools: ['database_read', 'database_write', 'evidence_retrieval', 'analytics_query'],
    inputSchema: { type: 'object', properties: { context: { type: 'string' }, goal: { type: 'object' } }, required: ['context'] },
    outputSchema: { type: 'object', properties: { findings: { type: 'array' }, recommendation: { type: 'string' }, confidence: { type: 'number' } }, required: ['recommendation', 'confidence'] },
    confidenceThreshold: 0.8,
    taskPermissions: ['READ_ONLY', 'PROPOSE_DECISION'],
    costBudget: 500,
    executionLimits: { maxRetries: 3, timeoutSeconds: 60, maxTokens: 4096 },
    performanceMetrics: {
      completionRate: 0.98,
      approvalRate: 0.95,
      avgLatencyMs: 1200,
      totalExecutions: 24,
      successfulExecutions: 23,
      failedExecutions: 1,
      costIncurredINR: 4.5
    },
    memoryScope: 'SYSTEM',
    evaluationCriteria: ['Factual accuracy', 'Strategic relevance', 'Actionability', 'Compliance safety'],
    failurePolicy: 'RETRY',
    retryPolicy: { maxAttempts: 3, backoffBaseMs: 1000 },
    status: 'IDLE',
    version: '1.0.0'
  },
  {
    id: 'anl-15',
    name: 'Trend Analysis Agent',
    category: 'ANALYTICS_LEARNING',
    role: 'Discovers week-over-week and month-over-month performance trends and seasonal cyclicality.',
    systemInstruction: `You are the ${'Trend Analysis Agent'}, operating within an autonomous AI Marketing Organization. Your core role is: ${'Discovers week-over-week and month-over-month performance trends and seasonal cyclicality.'}. Always deliver evidence-backed, actionable, and structured outputs tailored to the target market and business goals.`,
    capabilities: ['data_analysis', 'structured_synthesis', 'domain_reasoning'],
    allowedTools: ['database_read', 'database_write', 'evidence_retrieval', 'analytics_query'],
    inputSchema: { type: 'object', properties: { context: { type: 'string' }, goal: { type: 'object' } }, required: ['context'] },
    outputSchema: { type: 'object', properties: { findings: { type: 'array' }, recommendation: { type: 'string' }, confidence: { type: 'number' } }, required: ['recommendation', 'confidence'] },
    confidenceThreshold: 0.8,
    taskPermissions: ['READ_ONLY', 'PROPOSE_DECISION'],
    costBudget: 500,
    executionLimits: { maxRetries: 3, timeoutSeconds: 60, maxTokens: 4096 },
    performanceMetrics: {
      completionRate: 0.98,
      approvalRate: 0.95,
      avgLatencyMs: 1200,
      totalExecutions: 24,
      successfulExecutions: 23,
      failedExecutions: 1,
      costIncurredINR: 4.5
    },
    memoryScope: 'SYSTEM',
    evaluationCriteria: ['Factual accuracy', 'Strategic relevance', 'Actionability', 'Compliance safety'],
    failurePolicy: 'RETRY',
    retryPolicy: { maxAttempts: 3, backoffBaseMs: 1000 },
    status: 'IDLE',
    version: '1.0.0'
  },
  {
    id: 'anl-16',
    name: 'Forecasting Agent',
    category: 'ANALYTICS_LEARNING',
    role: 'Forecasts upcoming month lead volumes, appointment numbers, and required budgets based on run-rate data.',
    systemInstruction: `You are the ${'Forecasting Agent'}, operating within an autonomous AI Marketing Organization. Your core role is: ${'Forecasts upcoming month lead volumes, appointment numbers, and required budgets based on run-rate data.'}. Always deliver evidence-backed, actionable, and structured outputs tailored to the target market and business goals.`,
    capabilities: ['data_analysis', 'structured_synthesis', 'domain_reasoning'],
    allowedTools: ['database_read', 'database_write', 'evidence_retrieval', 'analytics_query'],
    inputSchema: { type: 'object', properties: { context: { type: 'string' }, goal: { type: 'object' } }, required: ['context'] },
    outputSchema: { type: 'object', properties: { findings: { type: 'array' }, recommendation: { type: 'string' }, confidence: { type: 'number' } }, required: ['recommendation', 'confidence'] },
    confidenceThreshold: 0.8,
    taskPermissions: ['READ_ONLY', 'PROPOSE_DECISION'],
    costBudget: 500,
    executionLimits: { maxRetries: 3, timeoutSeconds: 60, maxTokens: 4096 },
    performanceMetrics: {
      completionRate: 0.98,
      approvalRate: 0.95,
      avgLatencyMs: 1200,
      totalExecutions: 24,
      successfulExecutions: 23,
      failedExecutions: 1,
      costIncurredINR: 4.5
    },
    memoryScope: 'SYSTEM',
    evaluationCriteria: ['Factual accuracy', 'Strategic relevance', 'Actionability', 'Compliance safety'],
    failurePolicy: 'RETRY',
    retryPolicy: { maxAttempts: 3, backoffBaseMs: 1000 },
    status: 'IDLE',
    version: '1.0.0'
  },
  {
    id: 'anl-17',
    name: 'ROI Analyst',
    category: 'ANALYTICS_LEARNING',
    role: 'Computes return on investment (ROI) and return on ad spend (ROAS) across all organic and paid initiatives.',
    systemInstruction: `You are the ${'ROI Analyst'}, operating within an autonomous AI Marketing Organization. Your core role is: ${'Computes return on investment (ROI) and return on ad spend (ROAS) across all organic and paid initiatives.'}. Always deliver evidence-backed, actionable, and structured outputs tailored to the target market and business goals.`,
    capabilities: ['data_analysis', 'structured_synthesis', 'domain_reasoning'],
    allowedTools: ['database_read', 'database_write', 'evidence_retrieval', 'analytics_query'],
    inputSchema: { type: 'object', properties: { context: { type: 'string' }, goal: { type: 'object' } }, required: ['context'] },
    outputSchema: { type: 'object', properties: { findings: { type: 'array' }, recommendation: { type: 'string' }, confidence: { type: 'number' } }, required: ['recommendation', 'confidence'] },
    confidenceThreshold: 0.8,
    taskPermissions: ['READ_ONLY', 'PROPOSE_DECISION'],
    costBudget: 500,
    executionLimits: { maxRetries: 3, timeoutSeconds: 60, maxTokens: 4096 },
    performanceMetrics: {
      completionRate: 0.98,
      approvalRate: 0.95,
      avgLatencyMs: 1200,
      totalExecutions: 24,
      successfulExecutions: 23,
      failedExecutions: 1,
      costIncurredINR: 4.5
    },
    memoryScope: 'SYSTEM',
    evaluationCriteria: ['Factual accuracy', 'Strategic relevance', 'Actionability', 'Compliance safety'],
    failurePolicy: 'RETRY',
    retryPolicy: { maxAttempts: 3, backoffBaseMs: 1000 },
    status: 'IDLE',
    version: '1.0.0'
  },
  {
    id: 'anl-18',
    name: 'Experiment Evaluation Agent',
    category: 'ANALYTICS_LEARNING',
    role: 'Concludes experiments with rigorous decisions: SCALE, MODIFY, STOP, RERUN, or INCONCLUSIVE.',
    systemInstruction: `You are the ${'Experiment Evaluation Agent'}, operating within an autonomous AI Marketing Organization. Your core role is: ${'Concludes experiments with rigorous decisions: SCALE, MODIFY, STOP, RERUN, or INCONCLUSIVE.'}. Always deliver evidence-backed, actionable, and structured outputs tailored to the target market and business goals.`,
    capabilities: ['data_analysis', 'structured_synthesis', 'domain_reasoning'],
    allowedTools: ['database_read', 'database_write', 'evidence_retrieval', 'analytics_query'],
    inputSchema: { type: 'object', properties: { context: { type: 'string' }, goal: { type: 'object' } }, required: ['context'] },
    outputSchema: { type: 'object', properties: { findings: { type: 'array' }, recommendation: { type: 'string' }, confidence: { type: 'number' } }, required: ['recommendation', 'confidence'] },
    confidenceThreshold: 0.8,
    taskPermissions: ['READ_ONLY', 'PROPOSE_DECISION'],
    costBudget: 500,
    executionLimits: { maxRetries: 3, timeoutSeconds: 60, maxTokens: 4096 },
    performanceMetrics: {
      completionRate: 0.98,
      approvalRate: 0.95,
      avgLatencyMs: 1200,
      totalExecutions: 24,
      successfulExecutions: 23,
      failedExecutions: 1,
      costIncurredINR: 4.5
    },
    memoryScope: 'SYSTEM',
    evaluationCriteria: ['Factual accuracy', 'Strategic relevance', 'Actionability', 'Compliance safety'],
    failurePolicy: 'RETRY',
    retryPolicy: { maxAttempts: 3, backoffBaseMs: 1000 },
    status: 'IDLE',
    version: '1.0.0'
  },
  {
    id: 'anl-19',
    name: 'Insight Generation Agent',
    category: 'ANALYTICS_LEARNING',
    role: 'Transforms complex analytics data into clear "What Happened, Why, and What to Do Next" executive insights.',
    systemInstruction: `You are the ${'Insight Generation Agent'}, operating within an autonomous AI Marketing Organization. Your core role is: ${'Transforms complex analytics data into clear "What Happened, Why, and What to Do Next" executive insights.'}. Always deliver evidence-backed, actionable, and structured outputs tailored to the target market and business goals.`,
    capabilities: ['data_analysis', 'structured_synthesis', 'domain_reasoning'],
    allowedTools: ['database_read', 'database_write', 'evidence_retrieval', 'analytics_query'],
    inputSchema: { type: 'object', properties: { context: { type: 'string' }, goal: { type: 'object' } }, required: ['context'] },
    outputSchema: { type: 'object', properties: { findings: { type: 'array' }, recommendation: { type: 'string' }, confidence: { type: 'number' } }, required: ['recommendation', 'confidence'] },
    confidenceThreshold: 0.8,
    taskPermissions: ['READ_ONLY', 'PROPOSE_DECISION'],
    costBudget: 500,
    executionLimits: { maxRetries: 3, timeoutSeconds: 60, maxTokens: 4096 },
    performanceMetrics: {
      completionRate: 0.98,
      approvalRate: 0.95,
      avgLatencyMs: 1200,
      totalExecutions: 24,
      successfulExecutions: 23,
      failedExecutions: 1,
      costIncurredINR: 4.5
    },
    memoryScope: 'SYSTEM',
    evaluationCriteria: ['Factual accuracy', 'Strategic relevance', 'Actionability', 'Compliance safety'],
    failurePolicy: 'RETRY',
    retryPolicy: { maxAttempts: 3, backoffBaseMs: 1000 },
    status: 'IDLE',
    version: '1.0.0'
  },
  {
    id: 'anl-20',
    name: 'Learning Agent',
    category: 'ANALYTICS_LEARNING',
    role: 'Synthesizes validated insights into formal organizational learnings and updates strategy policies for subsequent cycles.',
    systemInstruction: `You are the ${'Learning Agent'}, operating within an autonomous AI Marketing Organization. Your core role is: ${'Synthesizes validated insights into formal organizational learnings and updates strategy policies for subsequent cycles.'}. Always deliver evidence-backed, actionable, and structured outputs tailored to the target market and business goals.`,
    capabilities: ['data_analysis', 'structured_synthesis', 'domain_reasoning'],
    allowedTools: ['database_read', 'database_write', 'evidence_retrieval', 'analytics_query'],
    inputSchema: { type: 'object', properties: { context: { type: 'string' }, goal: { type: 'object' } }, required: ['context'] },
    outputSchema: { type: 'object', properties: { findings: { type: 'array' }, recommendation: { type: 'string' }, confidence: { type: 'number' } }, required: ['recommendation', 'confidence'] },
    confidenceThreshold: 0.8,
    taskPermissions: ['READ_ONLY', 'PROPOSE_DECISION'],
    costBudget: 500,
    executionLimits: { maxRetries: 3, timeoutSeconds: 60, maxTokens: 4096 },
    performanceMetrics: {
      completionRate: 0.98,
      approvalRate: 0.95,
      avgLatencyMs: 1200,
      totalExecutions: 24,
      successfulExecutions: 23,
      failedExecutions: 1,
      costIncurredINR: 4.5
    },
    memoryScope: 'SYSTEM',
    evaluationCriteria: ['Factual accuracy', 'Strategic relevance', 'Actionability', 'Compliance safety'],
    failurePolicy: 'RETRY',
    retryPolicy: { maxAttempts: 3, backoffBaseMs: 1000 },
    status: 'IDLE',
    version: '1.0.0'
  }
];

export function getAgentById(id: string): AgentDescriptor | undefined {
  return AGENT_REGISTRY.find(a => a.id === id);
}

export function getAgentsByCategory(category: string): AgentDescriptor[] {
  return AGENT_REGISTRY.filter(a => a.category === category);
}